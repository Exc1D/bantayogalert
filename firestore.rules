rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Placeholder: full rules defined in Phase 2
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
