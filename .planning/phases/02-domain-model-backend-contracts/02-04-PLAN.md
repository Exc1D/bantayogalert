---
phase: 02-domain-model-backend-contracts
plan: '04'
type: execute
wave: 2
depends_on: ['02-03']
files_modified:
  - src/lib/geo/municipality.test.ts
  - src/components/map/TestMap.tsx
autonomous: true
requirements: [DM-06]
must_haves:
  truths:
    - "GeoJSON loads without errors and renders on test map"
---

<objective>
Verify municipalities.geojson loads correctly and renders on a Leaflet map. Create a minimal test component that renders the GeoJSON overlay.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Create unit test for loadMunicipalitiesGeoJSON</name>
  <files>src/lib/geo/municipality.test.ts</files>
  <action>
Create `src/lib/geo/municipality.test.ts`:

```typescript
import { describe, it, expect } from 'vitest'
import { MUNICIPALITIES, getMunicipality } from './municipality'

describe('municipality', () => {
  it('has all 12 Camarines Norte municipalities', () => {
    expect(MUNICIPALITIES).toHaveLength(12)
  })

  it('each municipality has required fields', () => {
    MUNICIPALITIES.forEach(m => {
      expect(m.code).toHaveLength(3)
      expect(m.name).toBeTruthy()
      expect(m.center.lat).toBeGreaterThan(13.8)
      expect(m.center.lat).toBeLessThan(14.8)
      expect(m.center.lng).toBeGreaterThan(122.3)
      expect(m.center.lng).toBeLessThan(123.3)
    })
  })

  it('getMunicipality returns correct municipality', () => {
    const daet = getMunicipality('daet')
    expect(daet?.name).toBe('Daet')
  })

  it('getMunicipality returns undefined for unknown code', () => {
    expect(getMunicipality('xxx')).toBeUndefined()
  })
})
```
  </action>
  <acceptance_criteria>
    - Test file exists and runs with npm run test -- --run
    - All 4 tests pass
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create test map component for GeoJSON render</name>
  <files>src/components/map/TestMap.tsx</files>
  <action>
Create `src/components/map/TestMap.tsx` — a simple Leaflet map that renders the GeoJSON overlay for manual verification:

```tsx
import { MapContainer, TileLayer, GeoJSON } from 'react-leaflet'
import { useEffect, useState } from 'react'
import { loadMunicipalitiesGeoJSON } from '@/lib/geo/municipality'

export function TestMap() {
  const [geojson, setGeojson] = useState<GeoJSON.GeoJSON | null>(null)

  useEffect(() => {
    loadMunicipalitiesGeoJSON().then(data => setGeojson(data as unknown as GeoJSON.GeoJSON))
  }, [])

  return (
    <MapContainer center={[14.15, 122.9]} zoom={10} style={{ height: 400, width: '100%' }}>
      <TileLayer url="https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png" />
      {geojson && <GeoJSON data={geojson} />}
    </MapContainer>
  )
}
```

Note: This component is for development/testing only — will be replaced by the real map shell in Phase 4.
  </action>
  <acceptance_criteria>
    - TestMap.tsx compiles without errors
    - File created in src/components/map/
  </acceptance_criteria>
</task>

</tasks>

<verification>
- npm run test -- --run passes all 4 municipality tests
- TestMap.tsx compiles without TypeScript errors
</verification>

<success_criteria>
- GeoJSON loads and renders correctly on Leaflet map
</success_criteria>
