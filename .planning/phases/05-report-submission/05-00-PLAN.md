---
phase: 05-report-submission
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - vitest.config.ts
  - tests/unit/report-form.test.ts
  - tests/unit/image-compression.test.ts
  - tests/unit/owner-status.test.ts
  - tests/integration/submit-report.test.ts
  - tests/e2e/report-submission.spec.ts
  - tests/e2e/my-reports.spec.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Test infrastructure exists before implementation plans run"
    - "Vitest configured with jsdom environment"
    - "Stub test files exist for all Phase 5 requirements"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Vitest test runner configuration"
    - path: "tests/unit/report-form.test.ts"
      provides: "Stub tests for form validation and coordinate bounds"
    - path: "tests/unit/image-compression.test.ts"
      provides: "Stub tests for browser-image-compression output"
    - path: "tests/unit/owner-status.test.ts"
      provides: "Stub tests for WORKFLOW_TO_OWNER_STATUS mapping"
    - path: "tests/integration/submit-report.test.ts"
      provides: "Stub integration tests for submitReport CF with emulator"
    - path: "tests/e2e/report-submission.spec.ts"
      provides: "Stub Playwright E2E for full form flow"
    - path: "tests/e2e/my-reports.spec.ts"
      provides: "Stub Playwright E2E for citizen track page"
---

<objective>
Create Phase 5 test infrastructure: vitest config, unit test stubs, integration test stubs, and E2E test stubs. These are the scaffolding that gets filled in during implementation. Nyquist Rule: tests must exist before the code they test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@package.json
@src/types/report.ts
@src/types/status.ts

**Test commands from package.json:**
- `npm run test` — Vitest unit tests
- `npm run test:ui` — Vitest with UI browser
- `npx playwright test` — E2E tests

**Key types for test stubs:**
- `IncidentType` enum: flood, landslide, fire, earthquake, medical, vehicle_accident, crime, other
- `Severity` enum: critical, high, medium, low
- `WORKFLOW_TO_OWNER_STATUS` mapping from src/types/status.ts
- `ReportStatus` enum: submitted, under_review, verified, etc.

**Testing patterns from existing codebase:**
- Vitest with `@testing-library/jest-dom` for DOM assertions
- Firebase Emulator for integration tests
- Playwright for E2E
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create vitest.config.ts</name>
  <files>vitest.config.ts</files>
  <action>
Create `vitest.config.ts` at project root with jsdom environment and proper globals setup.

```typescript
import { defineConfig } from 'vitest/config'
import react from '@vitejs/plugin-react'
import path from 'path'

export default defineConfig({
  plugins: [react()],
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: ['./tests/setup.ts'],
    include: ['src/**/*.test.{ts,tsx}', 'tests/**/*.test.{ts,tsx}'],
    coverage: {
      provider: 'v8',
      reporter: ['text', 'html'],
      exclude: ['node_modules/', 'tests/', '*.config.*'],
    },
  },
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
})
```

Also create `tests/setup.ts`:
```typescript
import '@testing-library/jest-dom'
```

This config enables `npm run test` with jsdom (for React component testing) and proper TypeScript/React aliases.
  </action>
  <verify>
<automated>test -f vitest.config.ts && grep -q "jsdom" vitest.config.ts && test -f tests/setup.ts</automated>
  </verify>
  <done>vitest.config.ts exists with jsdom environment and @ alias configured</done>
</task>

<task type="auto">
  <name>Task 2: Create unit test stubs</name>
  <files>tests/unit/report-form.test.ts, tests/unit/image-compression.test.ts, tests/unit/owner-status.test.ts</files>
  <action>
Create three stub unit test files. Each test file should have passing tests that will be expanded during implementation.

**`tests/unit/report-form.test.ts`** — Form validation and coordinate bounds:
```typescript
import { describe, it, expect } from 'vitest'

// RPT-04: Coordinate bounds validation
describe('Coordinate bounds validation', () => {
  it('accepts coordinates within Camarines Norte bounds', () => {
    const lat = 14.1
    const lng = 122.9
    expect(lat >= 13.8 && lat <= 14.8).toBe(true)
    expect(lng >= 122.3 && lng <= 123.3).toBe(true)
  })

  it('rejects lat below 13.8', () => {
    const lat = 13.5
    expect(lat >= 13.8).toBe(false)
  })

  it('rejects lng above 123.3', () => {
    const lng = 123.5
    expect(lng <= 123.3).toBe(false)
  })
})
```

**`tests/unit/image-compression.test.ts`** — Compression output validation (RPT-05):
```typescript
import { describe, it, expect } from 'vitest'

// RPT-05: Image compression — max 1MB, 1920px
describe('Image compression', () => {
  it('accepts file size <= 1MB after compression', () => {
    const maxBytes = 1 * 1024 * 1024
    // Stub: actual compression test fills in during implementation
    expect(true).toBe(true)
  })

  it('accepts dimensions <= 1920px longest edge', () => {
    const maxDim = 1920
    // Stub: actual compression test fills in during implementation
    expect(true).toBe(true)
  })
})
```

**`tests/unit/owner-status.test.ts`** — Owner status mapping (RPT-11):
```typescript
import { describe, it, expect } from 'vitest'

// RPT-11: Owner status label
describe('WORKFLOW_TO_OWNER_STATUS mapping', () => {
  it('maps pending to "Submitted"', () => {
    // Stub: actual WORKFLOW_TO_OWNER_STATUS test fills in during implementation
    expect(true).toBe(true)
  })

  it('maps verified to "Verified"', () => {
    expect(true).toBe(true)
  })
})
```
  </action>
  <verify>
<automated>test -f tests/unit/report-form.test.ts && test -f tests/unit/image-compression.test.ts && test -f tests/unit/owner-status.test.ts</automated>
  </verify>
  <done>Three unit test stubs exist and are runnable by vitest</done>
</task>

<task type="auto">
  <name>Task 3: Create integration test stub</name>
  <files>tests/integration/submit-report.test.ts</files>
  <action>
Create `tests/integration/submit-report.test.ts` — CF emulator test for three-doc atomic creation.

**RPT-06:** Three-tier report atomically created
**RPT-08:** Unverified reports hidden from public

```typescript
import { describe, it, expect, beforeAll } from 'vitest'
import { initializeApp } from 'firebase/app'
import { getFirestore, connectFirestoreEmulator } from 'firebase/firestore'
import { getAuth, connectAuthEmulator } from 'firebase/auth'

// Stub: actual emulator test fills in during implementation
describe('submitReport Cloud Function integration', () => {
  beforeAll(() => {
    // Setup emulator connections
  })

  it('creates three documents atomically', async () => {
    // Stub: actual test fills in during implementation
    expect(true).toBe(true)
  })

  it('public report has geohash, private report has exact coords', async () => {
    // Stub: actual test fills in during implementation
    expect(true).toBe(true)
  })

  it('unverified report not in public reports collection', async () => {
    // Stub: actual test fills in during implementation
    expect(true).toBe(true)
  })
})
```

This test requires Firebase emulators running. Tests should use `connectFirestoreEmulator` and `connectAuthEmulator` pointing to localhost ports from package.json scripts.
  </action>
  <verify>
<automated>test -f tests/integration/submit-report.test.ts && grep -q "submitReport" tests/integration/submit-report.test.ts</automated>
  </verify>
  <done>Integration test stub exists for submitReport CF three-doc creation</done>
</task>

<task type="auto">
  <name>Task 4: Create E2E test stubs</name>
  <files>tests/e2e/report-submission.spec.ts, tests/e2e/my-reports.spec.ts</files>
  <action>
Create two Playwright E2E test stubs.

**`tests/e2e/report-submission.spec.ts`** — Full form flow (RPT-01):
```typescript
import { test, expect } from '@playwright/test'

test.describe('Report Submission Flow', () => {
  test('4-step wizard renders and completes', async ({ page }) => {
    // Stub: actual E2E fills in during implementation
    await page.goto('/app/report')
    expect(true).toBe(true)
  })

  test('location picker renders with map', async ({ page }) => {
    // Stub: actual E2E fills in during implementation
    expect(true).toBe(true)
  })

  test('submit navigates to track page', async ({ page }) => {
    // Stub: actual E2E fills in during implementation
    expect(true).toBe(true)
  })
})
```

**`tests/e2e/my-reports.spec.ts`** — Track page (RPT-09, RPT-10):
```typescript
import { test, expect } from '@playwright/test'

test.describe('My Reports / Track Page', () => {
  test('shows submitted status immediately', async ({ page }) => {
    // Stub: actual E2E fills in during implementation
    await page.goto('/app/track/test-report-id')
    expect(true).toBe(true)
  })

  test('real-time updates reflect in UI', async ({ page }) => {
    // Stub: actual E2E fills in during implementation
    expect(true).toBe(true)
  })
})
```
  </action>
  <verify>
<automated>test -f tests/e2e/report-submission.spec.ts && test -f tests/e2e/my-reports.spec.ts</automated>
  </verify>
  <done>Two Playwright E2E test stubs exist and are discoverable by Playwright</done>
</task>

</tasks>

<verification>
- `npm run test -- --run` exits 0 with all stub tests passing
- `npx playwright test --dry-run` shows both E2E spec files
</verification>

<success_criteria>
- vitest.config.ts created with jsdom environment and @ alias
- tests/setup.ts created with @testing-library/jest-dom import
- tests/unit/report-form.test.ts stub exists
- tests/unit/image-compression.test.ts stub exists
- tests/unit/owner-status.test.ts stub exists
- tests/integration/submit-report.test.ts stub exists
- tests/e2e/report-submission.spec.ts stub exists
- tests/e2e/my-reports.spec.ts stub exists
</success_criteria>

<output>
After completion, create `.planning/phases/05-report-submission/05-00-SUMMARY.md`
</output>
