---
phase: 11-analytics-disaster-mapping
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/analytics.ts
  - src/types/audit.ts
  - functions/src/types/analytics.ts
  - functions/src/types/audit.ts
  - functions/src/types/index.ts
  - firestore.rules
  - firestore.indexes.json
autonomous: true
requirements:
  - ANL-03
  - ANL-04
  - ANL-05
  - ANL-06
must_haves:
  truths:
    - "Shared analytics and audit contracts exist on both the client and Cloud Functions sides"
    - "Firestore rules expose municipality analytics to matching municipal admins, province analytics to superadmins, and audit entries to admins within scope"
    - "Composite indexes exist for common audit filters by municipality, action, actor, and entity type"
  artifacts:
    - path: "src/types/analytics.ts"
      provides: "Client analytics contracts"
      contains: "AnalyticsSnapshot"
    - path: "src/types/audit.ts"
      provides: "Client audit contracts"
      contains: "AuditEntry"
    - path: "functions/src/types/analytics.ts"
      provides: "Server analytics contracts"
      contains: "AnalyticsSummaryCurrent"
    - path: "functions/src/types/audit.ts"
      provides: "Server audit contracts"
      contains: "AuditEntry"
    - path: "firestore.rules"
      provides: "Scoped read access for analytics and audit collections"
      contains: "match /analytics_municipality/{municipalityCode}"
    - path: "firestore.indexes.json"
      provides: "Audit query indexes"
      contains: "\"collectionGroup\": \"audit\""
  key_links:
    - from: "src/types/analytics.ts"
      to: "functions/src/types/analytics.ts"
      via: "Shared field names for aggregate docs"
      pattern: "byWorkflowState"
    - from: "firestore.rules"
      to: "src/hooks/useAuditLog.ts"
      via: "Audit collection queries can be filtered by municipalityCode, action, actorUid, entityType"
      pattern: "match /audit/{auditId}"
---

<objective>
Create the shared analytics/audit data contracts and scoped Firestore access rules before any aggregation writers or client hooks are introduced.
</objective>

<context>
@.planning/phases/11-analytics-disaster-mapping/11-CONTEXT.md
@SPECS.md
@firestore.rules
@firestore.indexes.json
@src/types/report.ts
@functions/src/types/report.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shared analytics and audit contracts for client and functions</name>
  <files>src/types/analytics.ts, src/types/audit.ts, functions/src/types/analytics.ts, functions/src/types/audit.ts, functions/src/types/index.ts</files>
  <read_first>
    - src/types/report.ts
    - src/types/status.ts
    - functions/src/types/report.ts
    - functions/src/types/index.ts
    - .planning/phases/11-analytics-disaster-mapping/11-CONTEXT.md
  </read_first>
  <action>
    Create `src/types/analytics.ts` and `functions/src/types/analytics.ts` with the same explicit aggregate shapes:
    - `AnalyticsWorkflowCounts` containing numeric fields for `total`, `pending`, `verified`, `rejected`, `dispatched`, `acknowledged`, `in_progress`, `resolved`
    - `AnalyticsDurationTotals` containing `verificationMinutesTotal`, `verificationCount`, `resolutionMinutesTotal`, `resolutionCount`
    - `HotspotCount` with `{ barangayCode, municipalityCode, count }`
    - `MunicipalityBreakdownEntry` with `{ municipalityCode, total, pending, verified, resolved, rejected }`
    - `AnalyticsSummaryCurrent` / `AnalyticsSnapshot` with fields:
      `scopeType`, `scopeCode`, `bucketKey`, `bucketType`, `byWorkflowState`, `byType`, `bySeverity`, `durationTotals`, `avgVerificationMinutes`, `avgResolutionMinutes`, `hotspots`, `municipalityBreakdown`, `updatedAt`

    Create `src/types/audit.ts` and `functions/src/types/audit.ts` with:
    - `AuditEntityType = 'report' | 'contact' | 'announcement' | 'user'`
    - `AuditAction` as string union values for `report_submit`, `triage_verify`, `triage_reject`, `triage_dispatch`, `triage_acknowledge`, `triage_in_progress`, `triage_resolve`, `triage_reroute`, `triage_update_priority`, `triage_update_notes`, `contact_create`, `contact_update`, `contact_deactivate`, `announcement_create`, `announcement_publish`, `announcement_cancel`, `user_role_set`
    - `AuditEntry` with `id`, `entityType`, `entityId`, `action`, `actorUid`, `actorRole`, `municipalityCode`, `provinceCode`, `createdAt`, `details`
    - `AuditFilters` with `municipalityCode`, `entityType`, `action`, `actorUid`, `dateFrom`, `dateTo`, `cursor`

    Update `functions/src/types/index.ts` to export `./analytics` and `./audit`.
  </action>
  <acceptance_criteria>
    - `src/types/analytics.ts` contains `export interface AnalyticsSnapshot`
    - `src/types/analytics.ts` contains `byWorkflowState`
    - `src/types/audit.ts` contains `export interface AuditEntry`
    - `functions/src/types/analytics.ts` contains `export interface AnalyticsSummaryCurrent`
    - `functions/src/types/audit.ts` contains `export type AuditEntityType`
    - `functions/src/types/index.ts` contains `export * from './analytics'`
    - `functions/src/types/index.ts` contains `export * from './audit'`
  </acceptance_criteria>
  <verify>
    <automated>grep -q "export interface AnalyticsSnapshot" src/types/analytics.ts && grep -q "export interface AuditEntry" src/types/audit.ts && grep -q "export \\* from './analytics'" functions/src/types/index.ts && grep -q "export \\* from './audit'" functions/src/types/index.ts</automated>
  </verify>
  <done>Client and server share explicit analytics and audit document contracts.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the partial analytics rule stub with scoped analytics and audit rules plus indexes</name>
  <files>firestore.rules, firestore.indexes.json</files>
  <read_first>
    - firestore.rules
    - firestore.indexes.json
    - .planning/phases/11-analytics-disaster-mapping/11-CONTEXT.md
    - SPECS.md
  </read_first>
  <action>
    In `firestore.rules`, replace the current `match /analytics/{municipalityCode}/daily/{date}` block with these exact collection families:

    1. `match /analytics_municipality/{municipalityCode}` with read access for `isProvincialSuperadmin()` or `isMunicipalAdmin(municipalityCode)` and nested `match /{bucketCollection}/{bucketId}` with the same read rule and `allow write: if false`
    2. `match /analytics_province/{provinceCode}` with read access only for `isProvincialSuperadmin()` and nested `match /{bucketCollection}/{bucketId}` with the same read rule and `allow write: if false`
    3. Replace the current audit rule with:
       - `allow read: if isProvincialSuperadmin() || (resource.data.municipalityCode != null && isMunicipalAdmin(resource.data.municipalityCode))`
       - `allow create, update, delete: if false`

    In `firestore.indexes.json`, add audit indexes with these field combinations:
    - `municipalityCode ASC, createdAt DESC`
    - `action ASC, createdAt DESC`
    - `actorUid ASC, createdAt DESC`
    - `entityType ASC, createdAt DESC`
    - `municipalityCode ASC, action ASC, createdAt DESC`
  </action>
  <acceptance_criteria>
    - `firestore.rules` contains `match /analytics_municipality/{municipalityCode}`
    - `firestore.rules` contains `match /analytics_province/{provinceCode}`
    - `firestore.rules` contains `resource.data.municipalityCode != null && isMunicipalAdmin(resource.data.municipalityCode)`
    - `firestore.indexes.json` contains `"collectionGroup": "audit"`
    - `firestore.indexes.json` contains `"fieldPath": "actorUid"`
    - `firestore.indexes.json` contains `"fieldPath": "entityType"`
  </acceptance_criteria>
  <verify>
    <automated>grep -q "match /analytics_municipality/{municipalityCode}" firestore.rules && grep -q "match /analytics_province/{provinceCode}" firestore.rules && grep -q '"collectionGroup": "audit"' firestore.indexes.json</automated>
  </verify>
  <done>Firestore exposes only scoped analytics/audit reads and has indexes for the planned audit viewer queries.</done>
</task>

</tasks>
