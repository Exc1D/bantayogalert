---
phase: 11-analytics-disaster-mapping
plan: '05'
type: execute
wave: 4
depends_on:
  - '11-02'
  - '11-04'
files_modified:
  - package.json
  - package-lock.json
  - src/components/analytics/AnalyticsDashboard.tsx
  - src/components/analytics/AnalyticsSummaryCards.tsx
  - src/components/analytics/AnalyticsDateRangeControls.tsx
  - src/components/analytics/AnalyticsScopeSelector.tsx
  - src/components/analytics/HotspotList.tsx
  - src/components/map/AnalyticsHeatmapOverlay.tsx
  - src/app/admin/analytics/page.tsx
autonomous: true
requirements:
  - ANL-01
  - ANL-02
  - ANL-03
  - ANL-04
  - ANL-05
must_haves:
  truths:
    - "Analytics dashboard renders KPI cards, type/severity/time charts, and timing stats from aggregate hooks"
    - "Recharts (or the approved equivalent) is installed and used for the dashboard charts"
    - "Desktop analytics can toggle a hotspot overlay on the persistent map without replacing the map instance"
  artifacts:
    - path: "src/components/analytics/AnalyticsDashboard.tsx"
      provides: "Main dashboard composition"
      contains: "ResponsiveContainer"
    - path: "src/components/map/AnalyticsHeatmapOverlay.tsx"
      provides: "Aggregate-driven disaster overlay"
      contains: "CircleMarker"
    - path: "src/app/admin/analytics/page.tsx"
      provides: "Analytics route page"
      contains: "AnalyticsDashboard"
  key_links:
    - from: "src/app/admin/analytics/page.tsx"
      to: "src/hooks/useAnalytics.ts"
      via: "Dashboard reads scoped aggregate data"
      pattern: "useAnalytics"
    - from: "src/components/map/AnalyticsHeatmapOverlay.tsx"
      to: "src/app/shell/DesktopShell.tsx"
      via: "Overlay renders on the existing persistent map"
      pattern: "MapContainerWrapper"
---

<objective>
Build the analytics dashboard UI and desktop-first disaster overlay on top of the aggregate data layer.
</objective>

<context>
@.planning/phases/11-analytics-disaster-mapping/11-CONTEXT.md
@.planning/phases/11-analytics-disaster-mapping/11-RESEARCH.md
@src/app/admin/analytics/page.tsx
@src/hooks/useAnalytics.ts
@src/components/report/AdminQueueFeed.tsx
@src/components/map/ReportMarkers.tsx
@src/components/map/MunicipalityBoundaries.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install the charting dependency and build the dashboard components</name>
  <files>package.json, package-lock.json, src/components/analytics/AnalyticsDashboard.tsx, src/components/analytics/AnalyticsSummaryCards.tsx, src/components/analytics/AnalyticsDateRangeControls.tsx, src/components/analytics/AnalyticsScopeSelector.tsx, src/components/analytics/HotspotList.tsx</files>
  <read_first>
    - package.json
    - src/hooks/useAnalytics.ts
    - src/components/report/AdminQueueFeed.tsx
    - .planning/phases/11-analytics-disaster-mapping/11-CONTEXT.md
    - .planning/phases/11-analytics-disaster-mapping/11-RESEARCH.md
  </read_first>
  <action>
    Add the `recharts` dependency to the root app package.

    Create the dashboard component set with these concrete elements:
    - `AnalyticsSummaryCards.tsx` rendering cards for `Total`, `Pending`, `Verified`, `Resolved`, `Rejected`
    - `AnalyticsDateRangeControls.tsx` rendering preset buttons `7D`, `30D`, `90D`, and `Custom`
    - `AnalyticsScopeSelector.tsx` rendering municipality drill-down only for `provincial_superadmin`
    - `HotspotList.tsx` rendering ranked barangay hotspot rows
    - `AnalyticsDashboard.tsx` using Recharts `ResponsiveContainer`, `BarChart`, `PieChart`, and `LineChart` to visualize `byType`, `bySeverity`, and time-series buckets, plus stat blocks for `avgVerificationMinutes` and `avgResolutionMinutes`
  </action>
  <acceptance_criteria>
    - `package.json` contains `recharts`
    - `src/components/analytics/AnalyticsDashboard.tsx` contains `ResponsiveContainer`
    - `src/components/analytics/AnalyticsDashboard.tsx` contains `BarChart`
    - `src/components/analytics/AnalyticsDashboard.tsx` contains `PieChart`
    - `src/components/analytics/AnalyticsDashboard.tsx` contains `LineChart`
    - `src/components/analytics/AnalyticsSummaryCards.tsx` contains `Total`
    - `src/components/analytics/AnalyticsSummaryCards.tsx` contains `Resolved`
    - `src/components/analytics/AnalyticsDateRangeControls.tsx` contains `30D`
  </acceptance_criteria>
  <verify>
    <automated>grep -q "recharts" package.json && grep -q "LineChart" src/components/analytics/AnalyticsDashboard.tsx && grep -q "30D" src/components/analytics/AnalyticsDateRangeControls.tsx</automated>
  </verify>
  <done>The analytics dashboard has concrete chart and KPI components backed by a React charting library.</done>
</task>

<task type="auto">
  <name>Task 2: Add the aggregate-driven disaster overlay and render the analytics page</name>
  <files>src/components/map/AnalyticsHeatmapOverlay.tsx, src/app/admin/analytics/page.tsx</files>
  <read_first>
    - src/app/admin/analytics/page.tsx
    - src/components/map/ReportMarkers.tsx
    - src/components/map/MunicipalityBoundaries.tsx
    - src/hooks/useAnalytics.ts
    - src/app/shell/DesktopShell.tsx
  </read_first>
  <action>
    Create `src/components/map/AnalyticsHeatmapOverlay.tsx` using existing `react-leaflet` primitives, not a separate heatmap plugin. Render hotspot intensity with `CircleMarker` layers derived from aggregate `hotspots`, scaling radius and opacity from `count`.

    Update `src/app/admin/analytics/page.tsx` to:
    - call `useAnalytics`
    - render `AnalyticsDashboard`
    - show a desktop-facing `Heatmap` toggle that mounts `AnalyticsHeatmapOverlay` onto the existing map context
    - keep mobile analytics summary-first by showing the dashboard content without requiring full heatmap interaction
  </action>
  <acceptance_criteria>
    - `src/components/map/AnalyticsHeatmapOverlay.tsx` contains `CircleMarker`
    - `src/components/map/AnalyticsHeatmapOverlay.tsx` contains `hotspots`
    - `src/app/admin/analytics/page.tsx` contains `AnalyticsDashboard`
    - `src/app/admin/analytics/page.tsx` contains `Heatmap`
    - `src/app/admin/analytics/page.tsx` contains `useAnalytics`
  </acceptance_criteria>
  <verify>
    <automated>grep -q "CircleMarker" src/components/map/AnalyticsHeatmapOverlay.tsx && grep -q "Heatmap" src/app/admin/analytics/page.tsx && grep -q "useAnalytics" src/app/admin/analytics/page.tsx</automated>
  </verify>
  <done>Desktop analytics exposes the required dashboard and a disaster overlay without replacing the persistent map.</done>
</task>

</tasks>
