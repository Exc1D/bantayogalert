---
phase: 11-analytics-disaster-mapping
plan: '04'
type: execute
wave: 3
depends_on:
  - '11-02'
  - '11-03'
files_modified:
  - src/hooks/useAnalytics.ts
  - src/hooks/useAuditLog.ts
  - src/app/admin/analytics/page.tsx
  - src/app/admin/audit/page.tsx
  - src/App.tsx
  - src/app/shell/DesktopShell.tsx
  - src/app/shell/MobileShell.tsx
autonomous: true
requirements:
  - ANL-03
  - ANL-04
  - ANL-05
  - ANL-06
must_haves:
  truths:
    - "Client hooks read analytics and audit data from dedicated collections only"
    - "Desktop and mobile admin routes expose analytics and audit as route-backed panels"
    - "Superadmin and municipal scopes flow from `useAuth()` into query keys and Firestore filters"
  artifacts:
    - path: "src/hooks/useAnalytics.ts"
      provides: "Analytics summary + timeseries query hook"
      contains: "ANALYTICS_QUERY_KEY"
    - path: "src/hooks/useAuditLog.ts"
      provides: "Audit log query hook"
      contains: "AUDIT_LOG_QUERY_KEY"
    - path: "src/App.tsx"
      provides: "Admin analytics and audit routes"
      contains: "/app/admin/analytics"
    - path: "src/app/shell/DesktopShell.tsx"
      provides: "Desktop navigation to analytics and audit"
      contains: "/app/admin/analytics"
  key_links:
    - from: "src/hooks/useAnalytics.ts"
      to: "analytics_municipality"
      via: "Direct aggregate doc reads"
      pattern: "analytics_municipality"
    - from: "src/hooks/useAuditLog.ts"
      to: "audit"
      via: "Filtered audit collection query"
      pattern: "collection(db, 'audit')"
---

<objective>
Create the client-side query layer and route wiring that Phase 11 UI work will build on.
</objective>

<context>
@.planning/phases/11-analytics-disaster-mapping/11-CONTEXT.md
@src/app/shell/DesktopShell.tsx
@src/app/shell/MobileShell.tsx
@src/hooks/useContacts.ts
@src/hooks/useAnnouncements.ts
@src/hooks/useAdminQueueListener.ts
@src/types/analytics.ts
@src/types/audit.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add aggregate-only analytics and audit hooks with scoped query keys</name>
  <files>src/hooks/useAnalytics.ts, src/hooks/useAuditLog.ts</files>
  <read_first>
    - src/hooks/useContacts.ts
    - src/hooks/useAnnouncements.ts
    - src/hooks/useAdminQueueListener.ts
    - src/types/analytics.ts
    - src/types/audit.ts
    - firestore.rules
  </read_first>
  <action>
    Create `src/hooks/useAnalytics.ts` exporting:
    - `ANALYTICS_QUERY_KEY(scopeType, scopeCode, rangeKey)`
    - `useAnalytics({ scopeType, scopeCode, dateRange })`

    The hook must read from:
    - `analytics_municipality/{municipalityCode}/summary/current`
    - `analytics_municipality/{municipalityCode}/daily/*`
    - `analytics_province/CMN/summary/current`
    - `analytics_province/CMN/daily/*`

    Create `src/hooks/useAuditLog.ts` exporting:
    - `AUDIT_LOG_QUERY_KEY(filters)`
    - `useAuditLog(filters)`

    The audit hook must query `collection(db, 'audit')`, always `orderBy('createdAt', 'desc')`, and conditionally apply `where` filters for `municipalityCode`, `entityType`, `action`, and `actorUid`, with cursor-based pagination.

    Neither hook may read from `reports` or `report_ops`.
  </action>
  <acceptance_criteria>
    - `src/hooks/useAnalytics.ts` contains `export const ANALYTICS_QUERY_KEY`
    - `src/hooks/useAnalytics.ts` contains `analytics_municipality`
    - `src/hooks/useAnalytics.ts` contains `analytics_province`
    - `src/hooks/useAuditLog.ts` contains `export const AUDIT_LOG_QUERY_KEY`
    - `src/hooks/useAuditLog.ts` contains `collection(db, 'audit')`
    - `src/hooks/useAuditLog.ts` contains `orderBy('createdAt', 'desc')`
    - neither file contains `collection(db, 'reports')`
    - neither file contains `collection(db, 'report_ops')`
  </acceptance_criteria>
  <verify>
    <automated>grep -q "export const ANALYTICS_QUERY_KEY" src/hooks/useAnalytics.ts && grep -q "collection(db, 'audit')" src/hooks/useAuditLog.ts && ! grep -q "collection(db, 'reports')" src/hooks/useAnalytics.ts</automated>
  </verify>
  <done>The client has aggregate-only query hooks for analytics and audit data.</done>
</task>

<task type="auto">
  <name>Task 2: Add route-backed admin analytics/audit pages and real navigation links</name>
  <files>src/app/admin/analytics/page.tsx, src/app/admin/audit/page.tsx, src/App.tsx, src/app/shell/DesktopShell.tsx, src/app/shell/MobileShell.tsx</files>
  <read_first>
    - src/App.tsx
    - src/app/shell/DesktopShell.tsx
    - src/app/shell/MobileShell.tsx
    - .planning/phases/11-analytics-disaster-mapping/11-CONTEXT.md
  </read_first>
  <action>
    Create placeholder route components:
    - `src/app/admin/analytics/page.tsx` exporting `AdminAnalyticsPage`
    - `src/app/admin/audit/page.tsx` exporting `AdminAuditPage`

    Update `src/App.tsx` to register:
    - `/app/admin/analytics`
    - `/app/admin/audit`

    Update `src/app/shell/DesktopShell.tsx` nav items to include visible admin-only links:
    - `Analytics` → `/app/admin/analytics`
    - `Audit` → `/app/admin/audit`

    Update `routeBackedPanelPaths` in `DesktopShell.tsx` to include both new routes.

    Update `src/app/shell/MobileShell.tsx` so the admin area recognises `/app/admin/analytics` and `/app/admin/audit` as route-backed content, and add tappable admin shortcuts for analytics and audit in the mobile admin/profile surface.
  </action>
  <acceptance_criteria>
    - `src/App.tsx` contains `/app/admin/analytics`
    - `src/App.tsx` contains `/app/admin/audit`
    - `src/app/shell/DesktopShell.tsx` contains `href: '/app/admin/analytics'`
    - `src/app/shell/DesktopShell.tsx` contains `href: '/app/admin/audit'`
    - `src/app/shell/MobileShell.tsx` contains `/app/admin/analytics`
    - `src/app/admin/analytics/page.tsx` contains `export function AdminAnalyticsPage`
    - `src/app/admin/audit/page.tsx` contains `export function AdminAuditPage`
  </acceptance_criteria>
  <verify>
    <automated>grep -q "/app/admin/analytics" src/App.tsx && grep -q "/app/admin/audit" src/App.tsx && grep -q "export function AdminAnalyticsPage" src/app/admin/analytics/page.tsx && grep -q "export function AdminAuditPage" src/app/admin/audit/page.tsx</automated>
  </verify>
  <done>The shell and router expose real analytics and audit destinations for the admin experience.</done>
</task>

</tasks>
