---
phase: 11-analytics-disaster-mapping
plan: '02'
type: execute
wave: 2
depends_on:
  - '11-01'
files_modified:
  - functions/src/analytics/shared.ts
  - functions/src/analytics/updateAnalyticsForStateChange.ts
  - functions/src/analytics/scheduledAggregation.ts
  - functions/src/index.ts
  - functions/src/reports/submitReport.ts
  - functions/src/triage/triageVerify.ts
  - functions/src/triage/triageReject.ts
  - functions/src/triage/triageDispatch.ts
  - functions/src/triage/triageAcknowledge.ts
  - functions/src/triage/triageInProgress.ts
  - functions/src/triage/triageResolve.ts
autonomous: true
requirements:
  - ANL-01
  - ANL-02
  - ANL-03
  - ANL-04
  - ANL-05
must_haves:
  truths:
    - "Analytics summaries and daily buckets are written from server-side mutation points only"
    - "Dashboard KPI counts and timing metrics come from aggregate docs, not client-side scans"
    - "A scheduled rollup runs at 02:00 Asia/Manila to maintain weekly/monthly buckets"
  artifacts:
    - path: "functions/src/analytics/updateAnalyticsForStateChange.ts"
      provides: "Shared aggregate writer for report lifecycle changes"
      contains: "updateAnalyticsForStateChange"
    - path: "functions/src/analytics/scheduledAggregation.ts"
      provides: "Scheduled weekly/monthly rollup"
      contains: "onSchedule"
    - path: "functions/src/index.ts"
      provides: "Exports scheduled analytics job"
      contains: "scheduledAggregation"
  key_links:
    - from: "functions/src/reports/submitReport.ts"
      to: "functions/src/analytics/updateAnalyticsForStateChange.ts"
      via: "submitReport writes the initial pending analytics state"
      pattern: "updateAnalyticsForStateChange"
    - from: "functions/src/triage/triageResolve.ts"
      to: "functions/src/analytics/updateAnalyticsForStateChange.ts"
      via: "resolve transition updates counts and duration metrics"
      pattern: "updateAnalyticsForStateChange"
---

<objective>
Implement the server-side analytics aggregation pipeline so Phase 11 dashboards can read prepared analytics snapshots instead of raw report collections.
</objective>

<context>
@.planning/phases/11-analytics-disaster-mapping/11-CONTEXT.md
@.planning/phases/11-analytics-disaster-mapping/11-RESEARCH.md
@functions/src/reports/submitReport.ts
@functions/src/triage/shared.ts
@functions/src/triage/triageVerify.ts
@functions/src/triage/triageResolve.ts
@functions/src/types/analytics.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a shared analytics reducer/writer for report lifecycle transitions</name>
  <files>functions/src/analytics/shared.ts, functions/src/analytics/updateAnalyticsForStateChange.ts</files>
  <read_first>
    - functions/src/types/analytics.ts
    - functions/src/types/report.ts
    - functions/src/reports/submitReport.ts
    - functions/src/triage/shared.ts
    - .planning/phases/11-analytics-disaster-mapping/11-RESEARCH.md
  </read_first>
  <action>
    Create `functions/src/analytics/shared.ts` with deterministic helpers:
    - `getDayKey(dateIso: string): string`
    - `getWeekKey(dateIso: string): string`
    - `getMonthKey(dateIso: string): string`
    - `buildScopeRefs(db, municipalityCode: string, provinceCode: string)` returning refs for municipality and province `summary/current` and `daily/{dayKey}` docs
    - `buildWorkflowDelta(previousState, nextState)` returning exact `FieldValue.increment()` deltas for `byWorkflowState`

    Create `functions/src/analytics/updateAnalyticsForStateChange.ts` exporting:
    - `updateAnalyticsForStateChange(db, payload)` where `payload` includes `reportId`, `municipalityCode`, `provinceCode`, `barangayCode`, `incidentType`, `severity`, `createdAt`, `previousState`, `nextState`, `verifiedAt`, `resolvedAt`

    The helper must write to both municipality and province scopes, updating:
    - `byWorkflowState`
    - `byType`
    - `bySeverity`
    - `durationTotals`
    - `avgVerificationMinutes`
    - `avgResolutionMinutes`
    - `hotspots`
    - `updatedAt`
  </action>
  <acceptance_criteria>
    - `functions/src/analytics/shared.ts` contains `export function getDayKey`
    - `functions/src/analytics/updateAnalyticsForStateChange.ts` contains `export async function updateAnalyticsForStateChange`
    - `functions/src/analytics/updateAnalyticsForStateChange.ts` contains `avgVerificationMinutes`
    - `functions/src/analytics/updateAnalyticsForStateChange.ts` contains `avgResolutionMinutes`
    - `functions/src/analytics/updateAnalyticsForStateChange.ts` contains `analytics_municipality`
    - `functions/src/analytics/updateAnalyticsForStateChange.ts` contains `analytics_province`
  </acceptance_criteria>
  <verify>
    <automated>grep -q "export async function updateAnalyticsForStateChange" functions/src/analytics/updateAnalyticsForStateChange.ts && grep -q "avgResolutionMinutes" functions/src/analytics/updateAnalyticsForStateChange.ts</automated>
  </verify>
  <done>The project has a single analytics writer that updates current summaries and daily aggregate docs for both scopes.</done>
</task>

<task type="auto">
  <name>Task 2: Call the analytics writer from submitReport and report state-transition functions</name>
  <files>functions/src/reports/submitReport.ts, functions/src/triage/triageVerify.ts, functions/src/triage/triageReject.ts, functions/src/triage/triageDispatch.ts, functions/src/triage/triageAcknowledge.ts, functions/src/triage/triageInProgress.ts, functions/src/triage/triageResolve.ts</files>
  <read_first>
    - functions/src/reports/submitReport.ts
    - functions/src/triage/triageVerify.ts
    - functions/src/triage/triageReject.ts
    - functions/src/triage/triageResolve.ts
    - functions/src/analytics/updateAnalyticsForStateChange.ts
    - functions/src/types/report.ts
  </read_first>
  <action>
    Import `updateAnalyticsForStateChange` and call it with these exact transition mappings:
    - `submitReport.ts`: `previousState: null`, `nextState: 'pending'`
    - `triageVerify.ts`: `previousState: 'pending'`, `nextState: 'verified'`
    - `triageReject.ts`: `previousState: 'pending'`, `nextState: 'rejected'`
    - `triageDispatch.ts`: `previousState: 'verified'`, `nextState: 'dispatched'`
    - `triageAcknowledge.ts`: `previousState: 'dispatched'`, `nextState: 'acknowledged'`
    - `triageInProgress.ts`: `previousState: 'acknowledged'`, `nextState: 'in_progress'`
    - `triageResolve.ts`: `previousState` read from current `report_ops`/`reports` state, `nextState: 'resolved'`

    Pass through `municipalityCode`, `provinceCode: 'CMN'`, `barangayCode`, `type`, `severity`, `createdAt`, and timing timestamps needed to compute averages.

    Do NOT add analytics writes to `triageUpdatePriority.ts`, `triageUpdateNotes.ts`, or `triageReroute.ts`; those actions should leave workflow counters unchanged.
  </action>
  <acceptance_criteria>
    - `functions/src/reports/submitReport.ts` contains `updateAnalyticsForStateChange`
    - `functions/src/triage/triageVerify.ts` contains `updateAnalyticsForStateChange`
    - `functions/src/triage/triageReject.ts` contains `updateAnalyticsForStateChange`
    - `functions/src/triage/triageDispatch.ts` contains `updateAnalyticsForStateChange`
    - `functions/src/triage/triageAcknowledge.ts` contains `updateAnalyticsForStateChange`
    - `functions/src/triage/triageInProgress.ts` contains `updateAnalyticsForStateChange`
    - `functions/src/triage/triageResolve.ts` contains `updateAnalyticsForStateChange`
  </acceptance_criteria>
  <verify>
    <automated>grep -q "updateAnalyticsForStateChange" functions/src/reports/submitReport.ts && grep -q "updateAnalyticsForStateChange" functions/src/triage/triageResolve.ts</automated>
  </verify>
  <done>All report lifecycle transitions update aggregate docs server-side, and non-state mutations leave counters untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Add the scheduled analytics rollup export</name>
  <files>functions/src/analytics/scheduledAggregation.ts, functions/src/index.ts</files>
  <read_first>
    - functions/src/index.ts
    - functions/src/analytics/updateAnalyticsForStateChange.ts
    - .planning/phases/11-analytics-disaster-mapping/11-RESEARCH.md
  </read_first>
  <action>
    Create `functions/src/analytics/scheduledAggregation.ts` using `onSchedule` from `firebase-functions/v2/scheduler` with:
    - `schedule: '0 2 * * *'`
    - `timeZone: 'Asia/Manila'`

    The function must iterate daily analytics docs and persist:
    - weekly aggregate docs under `weekly/{weekKey}`
    - monthly aggregate docs under `monthly/{monthKey}`
    - refreshed hotspot leaderboards in `summary/current`

    Export `scheduledAggregation` from `functions/src/index.ts`.
  </action>
  <acceptance_criteria>
    - `functions/src/analytics/scheduledAggregation.ts` contains `onSchedule`
    - `functions/src/analytics/scheduledAggregation.ts` contains `0 2 * * *`
    - `functions/src/analytics/scheduledAggregation.ts` contains `Asia/Manila`
    - `functions/src/index.ts` contains `scheduledAggregation`
  </acceptance_criteria>
  <verify>
    <automated>grep -q "onSchedule" functions/src/analytics/scheduledAggregation.ts && grep -q "Asia/Manila" functions/src/analytics/scheduledAggregation.ts && grep -q "scheduledAggregation" functions/src/index.ts</automated>
  </verify>
  <done>The backend exposes a daily analytics rollup job that maintains longer-range aggregate buckets.</done>
</task>

</tasks>
