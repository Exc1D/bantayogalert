---
phase: 11-analytics-disaster-mapping
plan: '03'
type: execute
wave: 2
depends_on:
  - '11-01'
files_modified:
  - functions/src/audit/shared.ts
  - functions/src/reports/submitReport.ts
  - functions/src/triage/triageVerify.ts
  - functions/src/triage/triageReject.ts
  - functions/src/triage/triageDispatch.ts
  - functions/src/triage/triageAcknowledge.ts
  - functions/src/triage/triageInProgress.ts
  - functions/src/triage/triageResolve.ts
  - functions/src/triage/triageReroute.ts
  - functions/src/triage/triageUpdatePriority.ts
  - functions/src/triage/triageUpdateNotes.ts
  - functions/src/contacts/createContact.ts
  - functions/src/contacts/updateContact.ts
  - functions/src/contacts/deactivateContact.ts
  - functions/src/announcements/createAnnouncement.ts
  - functions/src/announcements/publishAnnouncement.ts
  - functions/src/announcements/cancelAnnouncement.ts
  - functions/src/auth/setUserRole.ts
autonomous: true
requirements:
  - ANL-03
  - ANL-04
  - ANL-06
must_haves:
  truths:
    - "Sensitive write paths emit immutable audit entries into the global audit collection"
    - "Audit entries include entity type, action, actor, municipality, province, and details payload"
    - "Audit entries are written for reports, contacts, announcements, and user-role changes"
  artifacts:
    - path: "functions/src/audit/shared.ts"
      provides: "Audit entry helper"
      contains: "buildAuditEntry"
    - path: "functions/src/triage/triageVerify.ts"
      provides: "Report audit write"
      contains: "triage_verify"
    - path: "functions/src/contacts/createContact.ts"
      provides: "Contact audit write"
      contains: "contact_create"
    - path: "functions/src/announcements/publishAnnouncement.ts"
      provides: "Announcement audit write"
      contains: "announcement_publish"
    - path: "functions/src/auth/setUserRole.ts"
      provides: "User-role audit write"
      contains: "user_role_set"
  key_links:
    - from: "functions/src/audit/shared.ts"
      to: "functions/src/contacts/createContact.ts"
      via: "Shared audit helper used by contact mutations"
      pattern: "buildAuditEntry"
    - from: "functions/src/audit/shared.ts"
      to: "functions/src/announcements/publishAnnouncement.ts"
      via: "Shared audit helper used by announcement mutations"
      pattern: "buildAuditEntry"
---

<objective>
Create the global audit stream and retrofit every sensitive write path that Phase 11 needs for a usable audit viewer.
</objective>

<context>
@.planning/phases/11-analytics-disaster-mapping/11-CONTEXT.md
@functions/src/reports/submitReport.ts
@functions/src/triage/shared.ts
@functions/src/contacts/createContact.ts
@functions/src/announcements/publishAnnouncement.ts
@functions/src/auth/setUserRole.ts
@functions/src/types/audit.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a shared audit-entry builder for immutable audit documents</name>
  <files>functions/src/audit/shared.ts</files>
  <read_first>
    - functions/src/types/audit.ts
    - functions/src/triage/shared.ts
    - functions/src/auth/claims.ts
    - .planning/phases/11-analytics-disaster-mapping/11-CONTEXT.md
  </read_first>
  <action>
    Create `functions/src/audit/shared.ts` exporting:
    - `buildAuditEntry(payload)` returning an object with `id`, `entityType`, `entityId`, `action`, `actorUid`, `actorRole`, `municipalityCode`, `provinceCode`, `createdAt`, `details`
    - `appendAuditEntry(tx, db, payload)` that writes `tx.set(db.collection('audit').doc(entry.id), entry)`

    The helper must always default `provinceCode` to `'CMN'` and preserve `municipalityCode: null` for province-wide actions.
  </action>
  <acceptance_criteria>
    - `functions/src/audit/shared.ts` contains `export function buildAuditEntry`
    - `functions/src/audit/shared.ts` contains `export function appendAuditEntry`
    - `functions/src/audit/shared.ts` contains `db.collection('audit').doc`
    - `functions/src/audit/shared.ts` contains `provinceCode: payload.provinceCode ?? 'CMN'`
  </acceptance_criteria>
  <verify>
    <automated>grep -q "export function appendAuditEntry" functions/src/audit/shared.ts && grep -q "db.collection('audit').doc" functions/src/audit/shared.ts</automated>
  </verify>
  <done>The codebase has a reusable helper for consistent audit entries across all entity types.</done>
</task>

<task type="auto">
  <name>Task 2: Retrofit sensitive Cloud Functions to append audit entries with explicit action names</name>
  <files>functions/src/reports/submitReport.ts, functions/src/triage/*.ts, functions/src/contacts/createContact.ts, functions/src/contacts/updateContact.ts, functions/src/contacts/deactivateContact.ts, functions/src/announcements/createAnnouncement.ts, functions/src/announcements/publishAnnouncement.ts, functions/src/announcements/cancelAnnouncement.ts, functions/src/auth/setUserRole.ts</files>
  <read_first>
    - functions/src/reports/submitReport.ts
    - functions/src/triage/triageVerify.ts
    - functions/src/contacts/createContact.ts
    - functions/src/announcements/createAnnouncement.ts
    - functions/src/auth/setUserRole.ts
    - functions/src/audit/shared.ts
  </read_first>
  <action>
    Import `appendAuditEntry` and write one audit entry per successful mutation with these exact `action` strings:
    - `submitReport.ts` → `report_submit`
    - `triageVerify.ts` → `triage_verify`
    - `triageReject.ts` → `triage_reject`
    - `triageDispatch.ts` → `triage_dispatch`
    - `triageAcknowledge.ts` → `triage_acknowledge`
    - `triageInProgress.ts` → `triage_in_progress`
    - `triageResolve.ts` → `triage_resolve`
    - `triageReroute.ts` → `triage_reroute`
    - `triageUpdatePriority.ts` → `triage_update_priority`
    - `triageUpdateNotes.ts` → `triage_update_notes`
    - `createContact.ts` → `contact_create`
    - `updateContact.ts` → `contact_update`
    - `deactivateContact.ts` → `contact_deactivate`
    - `createAnnouncement.ts` → `announcement_create`
    - `publishAnnouncement.ts` → `announcement_publish`
    - `cancelAnnouncement.ts` → `announcement_cancel`
    - `setUserRole.ts` → `user_role_set`

    Use `entityType` values `report`, `contact`, `announcement`, and `user`, preserve the affected `entityId`, set `actorUid` from the authenticated caller, set `actorRole` from claims, and include action-specific structured `details`.
  </action>
  <acceptance_criteria>
    - `functions/src/reports/submitReport.ts` contains `report_submit`
    - `functions/src/triage/triageVerify.ts` contains `triage_verify`
    - `functions/src/contacts/createContact.ts` contains `contact_create`
    - `functions/src/announcements/publishAnnouncement.ts` contains `announcement_publish`
    - `functions/src/auth/setUserRole.ts` contains `user_role_set`
    - at least 10 mutation files contain `appendAuditEntry`
  </acceptance_criteria>
  <verify>
    <automated>grep -q "announcement_publish" functions/src/announcements/publishAnnouncement.ts && grep -q "user_role_set" functions/src/auth/setUserRole.ts && test "$(rg -l "appendAuditEntry" functions/src | wc -l)" -ge 10</automated>
  </verify>
  <done>The global audit stream is populated from all sensitive report/contact/announcement/admin mutations required by the viewer.</done>
</task>

</tasks>
