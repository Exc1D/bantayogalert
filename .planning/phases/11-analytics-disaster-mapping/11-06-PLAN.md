---
phase: 11-analytics-disaster-mapping
plan: '06'
type: execute
wave: 4
depends_on:
  - '11-03'
  - '11-04'
files_modified:
  - src/components/audit/AuditFilters.tsx
  - src/components/audit/AuditLogTable.tsx
  - src/app/admin/audit/page.tsx
autonomous: true
requirements:
  - ANL-03
  - ANL-04
  - ANL-06
must_haves:
  truths:
    - "Audit page renders a paginated table filtered by action, entity, actor, municipality, and date range"
    - "Expanded rows show the structured audit details payload for a selected entry"
    - "Audit viewer is route-backed and uses the scoped audit hook rather than report-local activity history"
  artifacts:
    - path: "src/components/audit/AuditFilters.tsx"
      provides: "Audit filter controls"
      contains: "entityType"
    - path: "src/components/audit/AuditLogTable.tsx"
      provides: "Audit rows and expansion UI"
      contains: "View details"
    - path: "src/app/admin/audit/page.tsx"
      provides: "Route-backed audit page"
      contains: "AuditLogTable"
  key_links:
    - from: "src/app/admin/audit/page.tsx"
      to: "src/hooks/useAuditLog.ts"
      via: "Page reads the global audit collection through the shared hook"
      pattern: "useAuditLog"
    - from: "src/components/audit/AuditLogTable.tsx"
      to: "src/types/audit.ts"
      via: "Expanded rows render the typed details payload"
      pattern: "AuditEntry"
---

<objective>
Build the route-backed audit log viewer UI that makes the new global audit stream inspectable by admins within their allowed scope.
</objective>

<context>
@.planning/phases/11-analytics-disaster-mapping/11-CONTEXT.md
@src/app/admin/audit/page.tsx
@src/hooks/useAuditLog.ts
@src/types/audit.ts
@src/components/report/AdminReportDetailPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create audit filter controls and a paginated, expandable audit table</name>
  <files>src/components/audit/AuditFilters.tsx, src/components/audit/AuditLogTable.tsx</files>
  <read_first>
    - src/hooks/useAuditLog.ts
    - src/types/audit.ts
    - src/components/report/AdminReportDetailPanel.tsx
    - .planning/phases/11-analytics-disaster-mapping/11-CONTEXT.md
  </read_first>
  <action>
    Create `AuditFilters.tsx` with controls for:
    - action
    - entity type
    - actor/user
    - municipality (visible only for superadmin-capable contexts)
    - date range

    Create `AuditLogTable.tsx` that:
    - renders rows from `useAuditLog`
    - orders newest first
    - supports cursor-based pagination
    - includes a `View details` button per row
    - expands rows inline to show the structured `details` payload
    - includes explicit empty and error states
  </action>
  <acceptance_criteria>
    - `src/components/audit/AuditFilters.tsx` contains `entityType`
    - `src/components/audit/AuditFilters.tsx` contains `actorUid`
    - `src/components/audit/AuditLogTable.tsx` contains `View details`
    - `src/components/audit/AuditLogTable.tsx` contains `createdAt`
    - `src/components/audit/AuditLogTable.tsx` contains `details`
    - `src/components/audit/AuditLogTable.tsx` contains `Load more`
  </acceptance_criteria>
  <verify>
    <automated>grep -q "View details" src/components/audit/AuditLogTable.tsx && grep -q "actorUid" src/components/audit/AuditFilters.tsx && grep -q "Load more" src/components/audit/AuditLogTable.tsx</automated>
  </verify>
  <done>The audit viewer has concrete filter controls and a browsable expandable table.</done>
</task>

<task type="auto">
  <name>Task 2: Render the route-backed audit page with scoped hook data</name>
  <files>src/app/admin/audit/page.tsx</files>
  <read_first>
    - src/app/admin/audit/page.tsx
    - src/hooks/useAuditLog.ts
    - src/components/audit/AuditFilters.tsx
    - src/components/audit/AuditLogTable.tsx
    - src/lib/auth/hooks.ts
  </read_first>
  <action>
    Update `src/app/admin/audit/page.tsx` to:
    - read `role` and `municipalityCode` from `useAuth()`
    - initialize filters with municipality locked for `municipal_admin`
    - pass filters into `useAuditLog`
    - render `AuditFilters` and `AuditLogTable`
    - show a title containing the text `Audit Log`
  </action>
  <acceptance_criteria>
    - `src/app/admin/audit/page.tsx` contains `useAuditLog`
    - `src/app/admin/audit/page.tsx` contains `AuditFilters`
    - `src/app/admin/audit/page.tsx` contains `AuditLogTable`
    - `src/app/admin/audit/page.tsx` contains `Audit Log`
    - `src/app/admin/audit/page.tsx` contains `municipalityCode`
  </acceptance_criteria>
  <verify>
    <automated>grep -q "useAuditLog" src/app/admin/audit/page.tsx && grep -q "Audit Log" src/app/admin/audit/page.tsx && grep -q "municipalityCode" src/app/admin/audit/page.tsx</automated>
  </verify>
  <done>The audit route renders the global audit stream through scoped filters instead of report-local activity history.</done>
</task>

</tasks>
