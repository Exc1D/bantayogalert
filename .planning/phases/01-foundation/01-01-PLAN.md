---
phase: 01-foundation
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - vite.config.ts
  - tailwind.config.js
  - postcss.config.js
  - index.html
  - src/main.tsx
  - src/App.tsx
  - src/index.css
  - .env.example
  - .gitignore
  - .eslintrc.cjs
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - SEC-05
  - SEC-06
user_setup: []

must_haves:
  truths:
    - "Vite dev server starts without errors"
    - "TypeScript compiles with strict mode enabled"
    - "Tailwind CSS classes render correctly"
    - "CI pipeline runs on push"
  artifacts:
    - path: package.json
      provides: "Project dependencies and scripts"
      exports:
        - dev
        - build
        - lint
        - typecheck
        - test:run
        - test:e2e
    - path: src/main.tsx
      provides: "React 18 entry point"
      contains: "createRoot"
    - path: src/App.tsx
      provides: "Root component with shell routing"
    - path: .github/workflows/ci.yml
      provides: "CI pipeline"
      contains: "ESLint"
      contains: "tsc --noEmit"
      contains: "vitest"
      contains: "playwright"
      contains: "vite build"
  key_links:
    - from: src/main.tsx
      to: src/App.tsx
      via: "render tree"
    - from: .github/workflows/ci.yml
      to: package.json
      via: "npm scripts"
---

<objective>
Set up the Vite + React 18 + Tailwind CSS + TypeScript project scaffold with strict mode, all base dependencies, and GitHub Actions CI pipeline. This is the foundation everything else builds on.

Purpose: Establish the project skeleton with correct tooling configuration
Output: Working dev server, passing lint/typecheck, CI pipeline defined
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@SPEC.md
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Vite + React 18 + TypeScript project</name>
  <files>package.json, tsconfig.json, vite.config.ts, index.html</files>
  <read_first></read_first>
  <action>
Create a Vite + React 18 + TypeScript project. Use `npm create vite@latest . -- --template react-ts` in the project root.

**tsconfig.json must use strict mode (per D-04):**
```json
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true
  },
  "include": ["src"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
```

**vite.config.ts:**
```typescript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  server: {
    port: 5173,
    host: true,
  },
})
```
  </action>
  <verify>
npm run dev starts on port 5173
tsc --noEmit exits 0
  </verify>
  <done>Vite dev server runs without errors; TypeScript strict mode active</done>
</task>

<task type="auto">
  <name>Task 2: Configure Tailwind CSS</name>
  <files>tailwind.config.js, postcss.config.js, src/index.css</files>
  <read_first></read_first>
  <action>
Install Tailwind CSS: `npm install -D tailwindcss postcss autoprefixer && npx tailwindcss init -p`

**tailwind.config.js:**
```javascript
/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {
      colors: {
        // Bantayog Alert color palette - to be refined in design phase
        primary: {
          50: '#f0f9ff',
          500: '#0ea5e9',
          900: '#0c4a6e',
        },
      },
    },
  },
  plugins: [],
}
```

**src/index.css (Tailwind directives):**
```css
@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  body {
    @apply antialiased;
  }
}
```
  </action>
  <verify>npm run build succeeds with Tailwind classes applied</verify>
  <done>Tailwind CSS configured and generating styles</done>
</task>

<task type="auto">
  <name>Task 3: Set up base project files and dependencies</name>
  <files>src/main.tsx, src/App.tsx, src/index.css, .env.example, .gitignore</files>
  <read_first></read_first>
  <action>
**src/main.tsx:**
```tsx
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import './index.css'
import App from './App.tsx'

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <App />
  </StrictMode>,
)
```

**src/App.tsx (placeholder shell):**
```tsx
function App() {
  return (
    <div className="min-h-screen bg-gray-50">
      <h1 className="text-2xl font-bold text-primary-900">Bantayog Alert</h1>
      <p className="text-gray-600">Foundation scaffold loading...</p>
    </div>
  )
}

export default App
```

**.env.example:**
```
VITE_FIREBASE_API_KEY=your_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
VITE_FIREBASE_APP_ID=your_app_id
```

**.gitignore (standard Node/Vite/React):**
```
node_modules
dist
.env
.env.local
.env.*.local
*.log
.DS_Store
coverage
.vite
```

**Additional dependencies to install:**
```
npm install react react-dom
npm install -D @types/react @types/react-dom @vitejs/plugin-react typescript vite eslint eslint-plugin-react-hooks eslint-plugin-react-refresh
```
  </action>
  <verify>npm run build succeeds; npm run dev starts</verify>
  <done>Base React 18 app renders in browser</done>
</task>

<task type="auto">
  <name>Task 4: Configure ESLint and Prettier</name>
  <files>.eslintrc.cjs, .prettierrc</files>
  <read_first></read_first>
  <action>
**.eslintrc.cjs:**
```js
module.exports = {
  root: true,
  env: { browser: true, es2020: true },
  extends: [
    'eslint:recommended',
    'plugin:@typescript-eslint/recommended',
    'plugin:react-hooks/recommended',
  ],
  ignorePatterns: ['dist', '.eslintrc.cjs'],
  parser: '@typescript-eslint/parser',
  plugins: ['react-refresh'],
  rules: {
    'react-refresh/only-export-components': [
      'warn',
      { allowConstantExport: true },
    ],
    '@typescript-eslint/no-unused-vars': ['error', { argsIgnorePattern: '^_' }],
  },
}
```

**.prettierrc:**
```json
{
  "semi": false,
  "singleQuote": true,
  "tabWidth": 2,
  "trailingComma": "es5",
  "printWidth": 100
}
```

Install prettier: `npm install -D prettier`
  </action>
  <verify>npm run lint passes with no errors</verify>
  <done>ESLint and Prettier configured and passing</done>
</task>

<task type="auto">
  <name>Task 5: Create GitHub Actions CI pipeline</name>
  <files>.github/workflows/ci.yml</files>
  <read_first></read_first>
  <action>
Create `.github/workflows/ci.yml` with the pipeline sequence from D-06: ESLint → `tsc --noEmit` → vitest → playwright → vite build

```yaml
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint:
    name: ESLint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - run: npm ci
      - run: npm run lint

  typecheck:
    name: TypeScript
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - run: npm ci
      - run: npm run typecheck

  test:
    name: Vitest
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - run: npm ci
      - run: npm run test:run

  test-e2e:
    name: Playwright
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - run: npm ci
      - run: npx playwright install --with-deps
      - run: npm run test:e2e

  build:
    name: Vite Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - run: npm ci
      - run: npm run build
```
  </action>
  <verify>
.github/workflows/ci.yml exists
Contains steps: ESLint, TypeScript, Vitest, Playwright, Vite Build
  </verify>
  <done>CI pipeline defined and ready to run on push/PR</done>
</task>

</tasks>

<verification>
- `npm run dev` starts dev server on port 5173
- `npm run build` produces dist/ output
- `npm run lint` exits 0
- `npm run typecheck` exits 0
- `.github/workflows/ci.yml` exists with all required jobs
</verification>

<success_criteria>
- Vite + React 18 app renders "Bantayog Alert" heading
- TypeScript strict mode active (noImplicitAny, strictNullChecks)
- Tailwind CSS generating styles
- CI pipeline defined (lint → typecheck → vitest → playwright → build)
- All base dependencies installed
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
