---
phase: 02-auth-role-model
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - src/contexts/AuthContext.tsx
autonomous: true
requirements:
  - AUTH-02
  - AUTH-03
  - AUTH-04
user_setup: []

must_haves:
  truths:
    - 'User can sign in with email/password and session persists across reloads'
    - 'User can sign out from any page'
    - 'AuthContext exposes role and municipality from Firebase custom claims'
    - 'IMPORTANT: Custom claims may be stale after role changes - users may need to re-authenticate'
  artifacts:
    - path: src/contexts/AuthContext.tsx
      provides: Full Firebase Auth integration
      min_lines: 80
    - path: src/hooks/useAuth.ts
      provides: useAuth hook (extracted from context)
      exports: ['useAuth']
  key_links:
    - from: src/contexts/AuthContext.tsx
      to: src/config/firebase.ts
      via: getFirebaseAuth()
      pattern: getFirebaseAuth
    - from: src/contexts/AuthContext.tsx
      to: Firestore
      via: onAuthStateChanged listener
      pattern: onAuthStateChanged
---

<objective>
Implement full Firebase Auth integration in AuthContext with session persistence, sign-in/sign-out, and custom claims extraction.
</objective>

<context>
@.planning/phases/02-auth-role-model/02-CONTEXT.md
@.planning/REQUIREMENTS.md
@src/config/firebase.ts
@src/contexts/AuthContext.tsx

**Phase requirement IDs:** AUTH-02, AUTH-03, AUTH-04

**User decisions from CONTEXT.md:**

- D-04: Roles are citizen, municipal_admin, provincial_superadmin (one role per user)
- D-05: Custom claims structure: { role: string, municipality: string | null }
- D-07: Custom claims set exclusively by privileged Cloud Function (never client-side)
- D-??: Stale custom claims limitation: Firebase ID tokens are cached. Users who are granted elevated roles may need to sign out and back in for new claims to take effect. This is a known Firebase limitation.
  </context>

<interfaces>
<!-- Key types and contracts from existing codebase -->

From src/contexts/AuthContext.tsx (current stub):

```typescript
interface User {
  uid: string
  email: string | null
  displayName: string | null
  role: 'citizen' | 'municipal_admin' | 'provincial_superadmin' | null
  municipality: string | null
}

interface AuthContextValue {
  user: User | null
  loading: boolean
  signIn: (email: string, password: string) => Promise<void>
  signOut: () => Promise<void>
}
```

From src/config/firebase.ts:

```typescript
export function getFirebaseAuth(): Auth
```

</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Implement Firebase Auth integration in AuthContext</name>
  <files>src/contexts/AuthContext.tsx</files>
  <action>
Replace the existing AuthContext.tsx stub with full Firebase Auth integration:

1. Import from firebase/auth: onAuthStateChanged, signInWithEmailAndPassword, signOut as firebaseSignOut
2. Import getFirebaseAuth from src/config/firebase.ts
3. Add signUp method: (email, password, displayName, municipality, phone) => Promise<void>
4. Add loading state: use useState(true) initially, set to false after first auth state resolution
5. Add useEffect with onAuthStateChanged listener that:
   - Sets user from auth state + custom claims
   - Extract role and municipality from auth.user custom claims (auth.token.role, auth.token.municipality)
   - Set loading to false when resolved
6. signIn: call signInWithEmailAndPassword(getFirebaseAuth(), email, password)
7. signOut: call firebaseSignOut(getFirebaseAuth())
8. On user change, fetch user document from Firestore to get additional profile data (displayName, municipality, phone)
9. User type must match: uid, email, displayName, role, municipality (from custom claims)

The User type already exists in the file — extend it if needed to match the SPEC.md User entity.
</action>
<read_first>

- src/config/firebase.ts (for getFirebaseAuth import)
- src/contexts/AuthContext.tsx (current stub to replace)
  </read_first>
  <verify>
  <automated>grep -n "onAuthStateChanged\|signInWithEmailAndPassword\|signOut" src/contexts/AuthContext.tsx | wc -l</automated>
  </verify>
  <acceptance_criteria> - AuthContext.tsx imports from firebase/auth - onAuthStateChanged listener sets user state - signIn method calls signInWithEmailAndPassword - signOut method calls firebaseSignOut - User type includes role and municipality from custom claims - loading state is true on mount, false after auth resolution - NOTE: Custom claims may be stale after role changes (user may need to re-authenticate)
  </acceptance_criteria>
  <done>
  AuthContext fully integrates Firebase Auth with session persistence via onAuthStateChanged. signIn/signOut work. Custom claims (role, municipality) are extracted from the Firebase token.
  </done>
  </task>

<task type="auto">
  <name>Task 2: Extract useAuth hook from AuthContext</name>
  <files>src/hooks/useAuth.ts</files>
  <action>
Create a new file src/hooks/useAuth.ts that re-exports useAuth from AuthContext:

```typescript
export { useAuth } from '../contexts/AuthContext'
```

This follows the established pattern in the codebase where hooks are in src/hooks/ and re-export from contexts.

Also add signUp to the AuthContext interface if not present.
</action>
<read_first>

- src/contexts/AuthContext.tsx
  </read_first>
  <verify>
  <automated>test -f src/hooks/useAuth.ts && grep -l "useAuth" src/hooks/useAuth.ts</automated>
  </verify>
  <acceptance_criteria> - src/hooks/useAuth.ts exists - File exports useAuth from AuthContext
  </acceptance_criteria>
  <done>
  useAuth hook available at src/hooks/useAuth.ts for cleaner imports in components.
  </done>
  </task>

</tasks>

<verification>
- npm run build succeeds with new AuthContext
- TypeScript compilation passes (tsc --noEmit)
- AuthContext exports useAuth hook
- signIn/signOut methods are async and properly typed
</verification>

<success_criteria>

- User can sign in with Firebase email/password auth
- Session persists across page reloads via onAuthStateChanged
- User can sign out
- AuthContext exposes user with role and municipality from custom claims
  </success_criteria>

<output>
After completion, create `.planning/phases/02-auth-role-model/02-01-SUMMARY.md`
</output>
