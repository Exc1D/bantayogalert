---
phase: 02-auth-role-model
plan: '04'
type: execute
wave: 2
depends_on: ['02-01', '02-03']
files_modified:
  - src/components/layout/DesktopShell.tsx
  - src/App.tsx
autonomous: true
requirements:
  - AUTH-05
user_setup: []

must_haves:
  truths:
    - 'Desktop shell routes to Feed for citizens'
    - 'Desktop shell routes to Admin panel for municipal_admin'
    - 'Desktop shell routes to Province overview for provincial_superadmin'
    - 'NavRail shows admin link only for admin roles'
  artifacts:
    - path: src/components/layout/DesktopShell.tsx
      provides: Role-aware desktop shell with appropriate default view
      min_lines: 60
  key_links:
    - from: DesktopShell.tsx
      to: src/contexts/AuthContext.tsx
      via: useAuth hook
      pattern: useAuth
    - from: DesktopShell.tsx
      to: src/components/auth/RoleGate.tsx
      via: Admin link visibility
      pattern: RoleGate
---

<objective>
Implement role-based post-authentication routing for desktop shell.
</objective>

<context>
@.planning/phases/02-auth-role-model/02-CONTEXT.md
@src/components/layout/DesktopShell.tsx
@src/contexts/AuthContext.tsx
@src/components/auth/RoleGate.tsx

**User decisions from CONTEXT.md:**

- D-08: Desktop routing: Citizens → Feed view, municipal_admin → Admin panel, provincial_superadmin → Province overview
- D-10: Mobile admin access: Admin panel accessible via Profile tab → role badge menu
  </context>

<interfaces>
Current DesktopShell structure:
```tsx
<div className="flex h-screen">
  <NavRail />
  <main className="flex-1 relative overflow-hidden">
    <MapCanvas />
    <RightModal />
  </main>
</div>
```

Current NavRail has placeholder items - needs admin-specific links.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update DesktopShell with role-aware default panel</name>
  <files>src/components/layout/DesktopShell.tsx</files>
  <action>
Update DesktopShell.tsx to implement role-based default panel:

1. Import useAuth from hooks
2. Import RoleGate from auth components
3. Import ModalContext to set default section on mount

4. Add useEffect that runs on mount:
   - For citizens: setModalSection('feed')
   - For municipal_admin: setModalSection('admin')
   - For provincial_superadmin: setModalSection('admin') (with province scope)

5. The shell structure remains:
   - NavRail on left (already exists)
   - MapCanvas always mounted
   - RightModal as sibling (already exists)

6. Default panel selection happens via ModalContext.setSection on mount

Do NOT change the overall shell structure - only add the initial routing logic.
</action>
<read_first>

- src/components/layout/DesktopShell.tsx
- src/contexts/AuthContext.tsx
- src/contexts/ModalContext.tsx
  </read_first>
  <verify>
  <automated>grep -c "useAuth\|role\|citizen\|admin" src/components/layout/DesktopShell.tsx</automated>
  </verify>
  <acceptance_criteria> - DesktopShell imports useAuth - useEffect runs on mount with role-based section selection - Citizens default to 'feed' section - Admin roles default to 'admin' section
  </acceptance_criteria>
  <done>
  DesktopShell auto-opens appropriate panel based on user role on first load.
  </done>
  </task>

<task type="auto">
  <name>Task 2: Update NavRail with admin-specific links</name>
  <files>src/components/layout/NavRail.tsx</files>
  <action>
Update NavRail.tsx to show admin-specific links only to authorized roles:

1. Import useAuth from hooks
2. Import RoleGate from auth components

3. Add admin-specific nav items that only show for municipal_admin and provincial_superadmin:
   - Contacts (opens 'contacts' section in modal)

4. The existing nav items (Feed, Map, Alerts, Profile) remain visible to all authenticated users

5. NavRail structure should remain 64px width with icons

Current nav items (from code inspection):

- Feed (home icon)
- Map (pin icon)
- Alerts (bell icon)
- Profile (user icon)

Add:

- Contacts (users icon) - wrapped in RoleGate with roles={['municipal_admin', 'provincial_superadmin']}
  </action>
  <read_first>
- src/components/layout/NavRail.tsx
- src/components/auth/RoleGate.tsx
  </read_first>
  <verify>
  <automated>grep -c "RoleGate\|Contacts\|contacts" src/components/layout/NavRail.tsx</automated>
  </verify>
  <acceptance_criteria> - NavRail imports useAuth and RoleGate - Contacts nav item wrapped in RoleGate - Only admin roles see the Contacts link
  </acceptance_criteria>
  <done>
  NavRail shows admin-specific links (Contacts) only for municipal_admin and provincial_superadmin roles.
  </done>
  </task>

</tasks>

<verification>
- npm run build succeeds
- TypeScript compilation passes
- Desktop routing logic works based on role
- Admin links hidden from citizens
</verification>

<success_criteria>

- Desktop shell opens appropriate panel based on user role
- Citizens see Feed as default view
- Admins see Admin panel as default view
- NavRail shows Contacts only for admin roles
  </success_criteria>

<output>
After completion, create `.planning/phases/02-auth-role-model/02-04-SUMMARY.md`
</output>
