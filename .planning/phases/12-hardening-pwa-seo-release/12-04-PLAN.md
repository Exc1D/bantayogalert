---
phase: 12-hardening-pwa-seo-release
plan: '04'
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/report/useReportDraft.ts
  - src/features/report/usePendingReportSubmission.ts
  - src/hooks/useConnectionStatus.ts
  - src/app/report/ReportForm.tsx
  - src/app/report/ReportFormPage.tsx
autonomous: true
requirements:
  - PWA-03
  - PWA-04
must_haves:
  truths:
    - "Offline or failed report submissions can be persisted separately from in-progress drafts"
    - "Queued submissions include a deterministic submission id, retry bookkeeping, and the original payload"
    - "Report submission screens can detect offline state and retry queued work when connectivity returns"
  artifacts:
    - path: "src/features/report/usePendingReportSubmission.ts"
      provides: "Queued submission storage helpers"
      contains: "pending-submissions"
    - path: "src/hooks/useConnectionStatus.ts"
      provides: "Shared online/offline state hook"
      contains: "navigator.onLine"
    - path: "src/app/report/ReportForm.tsx"
      provides: "Offline-aware report submit flow"
      contains: "retryQueued"
  key_links:
    - from: "src/features/report/usePendingReportSubmission.ts"
      to: "src/app/report/ReportForm.tsx"
      via: "Report form persists and retries queued submissions"
      pattern: "savePendingSubmission"
    - from: "src/hooks/useConnectionStatus.ts"
      to: "src/app/report/ReportForm.tsx"
      via: "Offline indicator and retry behavior are driven by shared connectivity state"
      pattern: "isOnline"
---

<objective>
Extend the report flow from draft-only persistence to real offline-aware submission recovery with a queued retry path.
</objective>

<context>
@.planning/phases/12-hardening-pwa-seo-release/12-CONTEXT.md
@.planning/phases/12-hardening-pwa-seo-release/12-RESEARCH.md
@src/features/report/useReportDraft.ts
@src/app/report/ReportForm.tsx
@src/app/report/ReportFormPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a persisted pending-submission queue next to the existing draft store</name>
  <files>src/features/report/useReportDraft.ts, src/features/report/usePendingReportSubmission.ts</files>
  <read_first>
    - src/features/report/useReportDraft.ts
    - src/features/report/ReportFormSchema.ts
    - .planning/phases/12-hardening-pwa-seo-release/12-CONTEXT.md
  </read_first>
  <action>
    Keep the existing `drafts` object store intact in `src/features/report/useReportDraft.ts`, and add a second store named `pending-submissions`.

    Create `src/features/report/usePendingReportSubmission.ts` exporting:
    - `QueuedReportSubmission` with `submissionId`, `userId`, `payload`, `files`, `retryCount`, `lastError`, `createdAt`, `updatedAt`
    - `savePendingSubmission`
    - `loadPendingSubmissions`
    - `removePendingSubmission`
    - `markPendingSubmissionAttempt`

    Persist entries by key `report-pending-{userId}-{submissionId}`.
  </action>
  <acceptance_criteria>
    - `src/features/report/useReportDraft.ts` contains `pending-submissions`
    - `src/features/report/usePendingReportSubmission.ts` contains `export interface QueuedReportSubmission`
    - `src/features/report/usePendingReportSubmission.ts` contains `report-pending-`
    - `src/features/report/usePendingReportSubmission.ts` contains `savePendingSubmission`
    - `src/features/report/usePendingReportSubmission.ts` contains `loadPendingSubmissions`
  </acceptance_criteria>
  <verify>
    <automated>grep -q "pending-submissions" src/features/report/useReportDraft.ts && grep -q "export interface QueuedReportSubmission" src/features/report/usePendingReportSubmission.ts && grep -q "report-pending-" src/features/report/usePendingReportSubmission.ts</automated>
  </verify>
  <done>The app can persist queued report submissions separately from plain form drafts.</done>
</task>

<task type="auto">
  <name>Task 2: Make report submission offline-aware and reconnect-retry capable</name>
  <files>src/hooks/useConnectionStatus.ts, src/app/report/ReportForm.tsx, src/app/report/ReportFormPage.tsx</files>
  <read_first>
    - src/app/report/ReportForm.tsx
    - src/app/report/ReportFormPage.tsx
    - src/features/report/usePendingReportSubmission.ts
    - src/lib/auth/AuthProvider.tsx
  </read_first>
  <action>
    Create `src/hooks/useConnectionStatus.ts` that tracks:
    - `isOnline`
    - `lastChangeAt`
    using `navigator.onLine` plus `window` `online` and `offline` events.

    Update `src/app/report/ReportForm.tsx` and the submit orchestration in `src/app/report/ReportFormPage.tsx` so:
    - every submission creates a deterministic `submissionId`
    - when the submit path fails because the browser is offline or a network request fails before completion, the form saves a `QueuedReportSubmission`
    - on mount and on transition back to `isOnline === true`, the app loads queued submissions for the current user and retries them
    - the form renders explicit offline messaging instead of silently failing
  </action>
  <acceptance_criteria>
    - `src/hooks/useConnectionStatus.ts` contains `navigator.onLine`
    - `src/hooks/useConnectionStatus.ts` contains `window.addEventListener('online'`
    - `src/app/report/ReportForm.tsx` contains `savePendingSubmission`
    - `src/app/report/ReportForm.tsx` contains `retryQueued`
    - `src/app/report/ReportForm.tsx` contains `isOnline`
  </acceptance_criteria>
  <verify>
    <automated>grep -q "navigator.onLine" src/hooks/useConnectionStatus.ts && grep -q "savePendingSubmission" src/app/report/ReportForm.tsx && grep -q "retryQueued" src/app/report/ReportForm.tsx && grep -q "isOnline" src/app/report/ReportForm.tsx</automated>
  </verify>
  <done>Report submission preserves failed/offline attempts and retries them when the app regains connectivity.</done>
</task>

</tasks>
