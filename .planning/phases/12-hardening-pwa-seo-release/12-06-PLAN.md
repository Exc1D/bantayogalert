---
phase: 12-hardening-pwa-seo-release
plan: '06'
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.example
  - src/lib/app-check/AppCheckProvider.tsx
  - src/lib/firebase/config.ts
  - src/test/setup.ts
autonomous: true
requirements:
  - SEC-01
must_haves:
  truths:
    - "Production App Check no longer uses the placeholder audit-only CustomProvider"
    - "Provider selection is controlled by explicit environment/config values so local and emulator flows still work"
    - "Tests and non-production builds can initialize the app without production site keys"
  artifacts:
    - path: ".env.example"
      provides: "Documented App Check env contract"
      contains: "VITE_APP_CHECK_MODE"
    - path: "src/lib/app-check/AppCheckProvider.tsx"
      provides: "Environment-gated production App Check provider"
      contains: "ReCaptchaEnterpriseProvider"
    - path: "src/lib/firebase/config.ts"
      provides: "Shared Firebase config awareness for App Check rollout"
      contains: "VITE_USE_EMULATOR"
  key_links:
    - from: ".env.example"
      to: "src/lib/app-check/AppCheckProvider.tsx"
      via: "Env keys drive provider selection and enforcement mode"
      pattern: "VITE_APP_CHECK_MODE"
    - from: "src/test/setup.ts"
      to: "src/lib/app-check/AppCheckProvider.tsx"
      via: "Tests mock or bypass App Check initialization safely"
      pattern: "AppCheck"
---

<objective>
Replace the temporary audit-mode App Check setup with a production-ready, rollout-controlled implementation that still preserves local and test ergonomics.
</objective>

<context>
@.planning/phases/12-hardening-pwa-seo-release/12-CONTEXT.md
@.planning/phases/12-hardening-pwa-seo-release/12-RESEARCH.md
@.env.example
@src/lib/app-check/AppCheckProvider.tsx
@src/lib/firebase/config.ts
@src/test/setup.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the App Check env contract and production provider selection</name>
  <files>.env.example, src/lib/app-check/AppCheckProvider.tsx</files>
  <read_first>
    - .env.example
    - src/lib/app-check/AppCheckProvider.tsx
    - .planning/phases/12-hardening-pwa-seo-release/12-CONTEXT.md
  </read_first>
  <action>
    Update `.env.example` to document these exact keys:
    - `VITE_APP_CHECK_MODE=audit`
    - `VITE_RECAPTCHA_ENTERPRISE_SITE_KEY=...`
    - `VITE_APP_CHECK_DEBUG_TOKEN=...`

    Rewrite `src/lib/app-check/AppCheckProvider.tsx` so:
    - production + `VITE_APP_CHECK_MODE=enforce` uses `ReCaptchaEnterpriseProvider`
    - emulator/development paths keep a debug or audit-safe provider path
    - the placeholder token generator is not used in enforced production mode
  </action>
  <acceptance_criteria>
    - `.env.example` contains `VITE_APP_CHECK_MODE`
    - `.env.example` contains `VITE_APP_CHECK_DEBUG_TOKEN`
    - `src/lib/app-check/AppCheckProvider.tsx` contains `ReCaptchaEnterpriseProvider`
    - `src/lib/app-check/AppCheckProvider.tsx` contains `VITE_APP_CHECK_MODE`
    - `src/lib/app-check/AppCheckProvider.tsx` contains `VITE_RECAPTCHA_ENTERPRISE_SITE_KEY`
  </acceptance_criteria>
  <verify>
    <automated>grep -q "VITE_APP_CHECK_MODE" .env.example && grep -q "VITE_APP_CHECK_DEBUG_TOKEN" .env.example && grep -q "ReCaptchaEnterpriseProvider" src/lib/app-check/AppCheckProvider.tsx && grep -q "VITE_APP_CHECK_MODE" src/lib/app-check/AppCheckProvider.tsx</automated>
  </verify>
  <done>App Check configuration is explicit and production provider selection is no longer hard-coded to audit mode.</done>
</task>

<task type="auto">
  <name>Task 2: Make App Check rollout safe for emulators, tests, and production builds</name>
  <files>src/lib/app-check/AppCheckProvider.tsx, src/lib/firebase/config.ts, src/test/setup.ts</files>
  <read_first>
    - src/lib/app-check/AppCheckProvider.tsx
    - src/lib/firebase/config.ts
    - src/test/setup.ts
  </read_first>
  <action>
    Update `src/lib/firebase/config.ts` and `src/lib/app-check/AppCheckProvider.tsx` so the App Check provider path respects `VITE_USE_EMULATOR === 'true'` and never requires production site keys in emulator/test mode.

    Update `src/test/setup.ts` with a stable App Check mock/bypass so component tests do not fail when App Check initialization code is imported.
  </action>
  <acceptance_criteria>
    - `src/lib/app-check/AppCheckProvider.tsx` contains `VITE_USE_EMULATOR`
    - `src/lib/app-check/AppCheckProvider.tsx` contains `VITE_APP_CHECK_DEBUG_TOKEN`
    - `src/test/setup.ts` contains `AppCheck`
    - `src/test/setup.ts` contains `app-check`
  </acceptance_criteria>
  <verify>
    <automated>grep -q "VITE_USE_EMULATOR" src/lib/app-check/AppCheckProvider.tsx && grep -q "VITE_APP_CHECK_DEBUG_TOKEN" src/lib/app-check/AppCheckProvider.tsx && grep -q "AppCheck" src/test/setup.ts</automated>
  </verify>
  <done>App Check rollout is compatible with local development, testing, and staged production enforcement.</done>
</task>

</tasks>
