---
phase: 12-hardening-pwa-seo-release
plan: '05'
type: execute
wave: 3
depends_on:
  - '12-04'
files_modified:
  - vite.config.ts
  - public/offline.html
  - src/app/providers.tsx
  - src/components/pwa/ConnectionStatusBanner.tsx
  - src/components/pwa/InstallPromptBanner.tsx
  - src/hooks/useInstallPrompt.ts
autonomous: true
requirements:
  - PWA-03
  - PWA-04
must_haves:
  truths:
    - "The service worker has a real offline fallback document for navigation failures"
    - "The app exposes a shared connection-status banner outside the report form"
    - "Install prompt handling fits the existing prompt-based PWA registration model"
  artifacts:
    - path: "public/offline.html"
      provides: "Offline navigation fallback"
      contains: "You are offline"
    - path: "src/components/pwa/ConnectionStatusBanner.tsx"
      provides: "Shared offline indicator"
      contains: "Offline"
    - path: "src/hooks/useInstallPrompt.ts"
      provides: "Install prompt hook"
      contains: "beforeinstallprompt"
  key_links:
    - from: "vite.config.ts"
      to: "public/offline.html"
      via: "Workbox navigation fallback points to the offline document"
      pattern: "offline.html"
    - from: "src/app/providers.tsx"
      to: "src/components/pwa/ConnectionStatusBanner.tsx"
      via: "Shared provider tree renders the connection banner for the whole app"
      pattern: "ConnectionStatusBanner"
---

<objective>
Finish the PWA runtime experience by adding offline navigation fallback, shared connection-state UI, and install prompt handling.
</objective>

<context>
@.planning/phases/12-hardening-pwa-seo-release/12-CONTEXT.md
@.planning/phases/12-hardening-pwa-seo-release/12-RESEARCH.md
@vite.config.ts
@src/app/providers.tsx
@src/hooks/useConnectionStatus.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add an offline fallback document and wire it into the existing Workbox config</name>
  <files>vite.config.ts, public/offline.html</files>
  <read_first>
    - vite.config.ts
    - public/manifest.json
    - .planning/phases/12-hardening-pwa-seo-release/12-RESEARCH.md
  </read_first>
  <action>
    Create `public/offline.html` containing:
    - a clear "You are offline" headline
    - guidance to reopen the app when connectivity returns
    - a link back to `/`

    Update `vite.config.ts` so the existing `VitePWA` GenerateSW config precaches `offline.html` and uses it as the navigation fallback while preserving the current NetworkFirst Firestore caching and CacheFirst tile caching behavior.
  </action>
  <acceptance_criteria>
    - `public/offline.html` contains `You are offline`
    - `public/offline.html` contains `href="/" `
    - `vite.config.ts` contains `offline.html`
    - `vite.config.ts` still contains `firestore-cache`
    - `vite.config.ts` still contains `map-tiles-cache`
  </acceptance_criteria>
  <verify>
    <automated>grep -q "You are offline" public/offline.html && grep -q "offline.html" vite.config.ts && grep -q "firestore-cache" vite.config.ts && grep -q "map-tiles-cache" vite.config.ts</automated>
  </verify>
  <done>The service worker can serve an explicit offline fallback document without losing the existing caching strategy.</done>
</task>

<task type="auto">
  <name>Task 2: Add shared connection and install prompt UI to the app provider tree</name>
  <files>src/app/providers.tsx, src/components/pwa/ConnectionStatusBanner.tsx, src/components/pwa/InstallPromptBanner.tsx, src/hooks/useInstallPrompt.ts</files>
  <read_first>
    - src/app/providers.tsx
    - src/hooks/useConnectionStatus.ts
    - .planning/phases/12-hardening-pwa-seo-release/12-CONTEXT.md
  </read_first>
  <action>
    Create `src/hooks/useInstallPrompt.ts` around the browser `beforeinstallprompt` event and expose:
    - `isInstallAvailable`
    - `promptInstall`
    - `dismissInstallPrompt`

    Create `src/components/pwa/ConnectionStatusBanner.tsx` that renders a persistent offline status bar when `isOnline` is false.

    Create `src/components/pwa/InstallPromptBanner.tsx` that renders an install CTA only when `isInstallAvailable` is true.

    Update `src/app/providers.tsx` to render both banners above the routed app content.
  </action>
  <acceptance_criteria>
    - `src/hooks/useInstallPrompt.ts` contains `beforeinstallprompt`
    - `src/hooks/useInstallPrompt.ts` contains `promptInstall`
    - `src/components/pwa/ConnectionStatusBanner.tsx` contains `Offline`
    - `src/components/pwa/InstallPromptBanner.tsx` contains `Install`
    - `src/app/providers.tsx` contains `ConnectionStatusBanner`
    - `src/app/providers.tsx` contains `InstallPromptBanner`
  </acceptance_criteria>
  <verify>
    <automated>grep -q "beforeinstallprompt" src/hooks/useInstallPrompt.ts && grep -q "Offline" src/components/pwa/ConnectionStatusBanner.tsx && grep -q "Install" src/components/pwa/InstallPromptBanner.tsx && grep -q "ConnectionStatusBanner" src/app/providers.tsx && grep -q "InstallPromptBanner" src/app/providers.tsx</automated>
  </verify>
  <done>The app-wide provider tree now exposes both offline state and install prompt UX.</done>
</task>

</tasks>
