---
phase: 12-hardening-pwa-seo-release
plan: '07'
type: execute
wave: 4
depends_on:
  - '12-01'
  - '12-02'
  - '12-04'
  - '12-05'
  - '12-06'
files_modified:
  - src/App.tsx
  - src/App.test.tsx
  - src/app/auth/layout.tsx
  - src/app/auth/login/page.tsx
  - src/app/shell/DesktopShell.tsx
  - src/app/shell/MobileShell.tsx
  - src/app/report/ReportForm.tsx
  - src/app/public/landing/page.tsx
  - package.json
autonomous: true
requirements:
  - SEO-01
  - SEO-02
  - PWA-04
  - SEC-01
must_haves:
  truths:
    - "Critical public and private surfaces have semantic landmarks, icon-button labels, and keyboard/focus-safe navigation"
    - "Heavy routes/components can be lazy-loaded so the release build does not keep every surface in the initial bundle"
    - "Release verification covers the new public routes and metadata behavior"
  artifacts:
    - path: "src/app/auth/layout.tsx"
      provides: "Semantic auth layout"
      contains: "<main"
    - path: "src/App.tsx"
      provides: "Lazy-loaded route wiring"
      contains: "lazy("
    - path: "src/App.test.tsx"
      provides: "Route and metadata smoke coverage"
      contains: "public map"
  key_links:
    - from: "src/App.tsx"
      to: "src/App.test.tsx"
      via: "Route wiring changes are covered by smoke tests"
      pattern: "/public/map"
    - from: "src/app/shell/DesktopShell.tsx"
      to: "src/app/shell/MobileShell.tsx"
      via: "Shared accessibility/focus expectations apply across both shell variants"
      pattern: "aria-label"
---

<objective>
Finish the release hardening pass with targeted accessibility and performance remediation, then update smoke coverage to verify the new public route behavior.
</objective>

<context>
@.planning/phases/12-hardening-pwa-seo-release/12-CONTEXT.md
@.planning/phases/12-hardening-pwa-seo-release/12-RESEARCH.md
@.planning/phases/12-hardening-pwa-seo-release/12-VALIDATION.md
@src/App.tsx
@src/App.test.tsx
@src/app/auth/layout.tsx
@src/app/shell/DesktopShell.tsx
@src/app/shell/MobileShell.tsx
@src/app/report/ReportForm.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Patch the highest-value accessibility gaps in auth, shell, report, and public surfaces</name>
  <files>src/app/auth/layout.tsx, src/app/auth/login/page.tsx, src/app/shell/DesktopShell.tsx, src/app/shell/MobileShell.tsx, src/app/report/ReportForm.tsx, src/app/public/landing/page.tsx</files>
  <read_first>
    - src/app/auth/layout.tsx
    - src/app/auth/login/page.tsx
    - src/app/shell/DesktopShell.tsx
    - src/app/shell/MobileShell.tsx
    - src/app/report/ReportForm.tsx
    - SPECS.md
  </read_first>
  <action>
    Update the listed files so they use explicit semantic landmarks and accessible control labels:
    - auth and public layouts expose `<main>`
    - desktop shell exposes `<nav>` and labeled icon-only navigation buttons
    - mobile tab buttons have explicit `aria-label` text
    - report form status and submission/offline messages use an `aria-live` region
    - any icon-only or close/cancel controls in these files have explicit `aria-label` values
  </action>
  <acceptance_criteria>
    - `src/app/auth/layout.tsx` contains `<main`
    - `src/app/shell/DesktopShell.tsx` contains `<nav`
    - `src/app/shell/DesktopShell.tsx` contains `aria-label`
    - `src/app/shell/MobileShell.tsx` contains `aria-label`
    - `src/app/report/ReportForm.tsx` contains `aria-live`
  </acceptance_criteria>
  <verify>
    <automated>grep -q "<main" src/app/auth/layout.tsx && grep -q "<nav" src/app/shell/DesktopShell.tsx && grep -q "aria-label" src/app/shell/DesktopShell.tsx && grep -q "aria-label" src/app/shell/MobileShell.tsx && grep -q "aria-live" src/app/report/ReportForm.tsx</automated>
  </verify>
  <done>The critical user journeys have concrete semantic and labeling improvements for keyboard and screen-reader use.</done>
</task>

<task type="auto">
  <name>Task 2: Add route-level lazy loading and expand smoke coverage for public release surfaces</name>
  <files>src/App.tsx, src/App.test.tsx, package.json</files>
  <read_first>
    - src/App.tsx
    - src/App.test.tsx
    - package.json
    - .planning/phases/12-hardening-pwa-seo-release/12-VALIDATION.md
  </read_first>
  <action>
    Update `src/App.tsx` to lazy-load the heaviest non-root route surfaces with `React.lazy` and `Suspense`, prioritizing:
    - public map
    - public alerts/detail
    - admin analytics
    - admin audit

    Update `src/App.test.tsx` to cover:
    - landing page route
    - public map route
    - public alerts route
    - private-route noindex behavior or metadata helper usage

    Add a release verification script in `package.json` named `verify:release` that runs `npm run lint`, `npm run build`, and `npx vitest run`.
  </action>
  <acceptance_criteria>
    - `src/App.tsx` contains `lazy(`
    - `src/App.tsx` contains `Suspense`
    - `src/App.test.tsx` contains `public map`
    - `src/App.test.tsx` contains `public alerts`
    - `package.json` contains `verify:release`
  </acceptance_criteria>
  <verify>
    <automated>grep -q "lazy(" src/App.tsx && grep -q "Suspense" src/App.tsx && grep -q "public map" src/App.test.tsx && grep -q "public alerts" src/App.test.tsx && grep -q '"verify:release"' package.json</automated>
  </verify>
  <done>The release hardening pass ends with lighter route loading and smoke coverage for the new public surfaces.</done>
</task>

</tasks>
