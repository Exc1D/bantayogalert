---
phase: 12-hardening-pwa-seo-release
plan: '03'
type: execute
wave: 3
depends_on:
  - '12-01'
  - '12-02'
files_modified:
  - public/robots.txt
  - public/sitemap.xml
  - public/og-image.svg
  - firebase.json
  - functions/src/public/renderPublicAlertMeta.ts
  - functions/src/index.ts
autonomous: true
requirements:
  - SEO-03
  - SEO-04
  - SEO-06
must_haves:
  truths:
    - "Static SEO assets exist for robots and sitemap"
    - "A Hosting rewrite routes public alert share requests to an HTTP function"
    - "The HTTP function returns alert-specific title, description, canonical, and OG tags for published alerts only"
  artifacts:
    - path: "public/robots.txt"
      provides: "Crawler policy"
      contains: "Disallow: /app/"
    - path: "public/sitemap.xml"
      provides: "Static sitemap for v1 public surfaces"
      contains: "/public/alerts"
    - path: "functions/src/public/renderPublicAlertMeta.ts"
      provides: "Dynamic public alert OG handler"
      contains: "og:title"
  key_links:
    - from: "firebase.json"
      to: "functions/src/public/renderPublicAlertMeta.ts"
      via: "Hosting rewrite targets the alert metadata function"
      pattern: "renderPublicAlertMeta"
    - from: "functions/src/public/renderPublicAlertMeta.ts"
      to: "src/app/public/alerts/detail/page.tsx"
      via: "Function emits canonical/public alert URLs that match the SPA route"
      pattern: "/public/alerts/"
---

<objective>
Ship the static SEO assets and dynamic public-alert sharing path so crawlers and social previews can resolve the intended public surfaces.
</objective>

<context>
@.planning/phases/12-hardening-pwa-seo-release/12-CONTEXT.md
@.planning/phases/12-hardening-pwa-seo-release/12-RESEARCH.md
@firebase.json
@functions/src/index.ts
@src/app/public/alerts/detail/page.tsx
@SPECS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add static SEO assets and Hosting rewrite configuration</name>
  <files>public/robots.txt, public/sitemap.xml, public/og-image.svg, firebase.json</files>
  <read_first>
    - firebase.json
    - SPECS.md
    - .planning/phases/12-hardening-pwa-seo-release/12-CONTEXT.md
  </read_first>
  <action>
    Create `public/robots.txt` with these exact directives:
    - `User-agent: *`
    - `Allow: /`
    - `Allow: /public/`
    - `Disallow: /app/`
    - `Disallow: /auth/`
    - `Disallow: /admin/`
    - `Disallow: /profile/`

    Create `public/sitemap.xml` with URLs for `/`, `/public/map`, and `/public/alerts`.

    Create a static social preview asset at `public/og-image.svg` for the landing/public routes.

    Update `firebase.json` Hosting config to add a rewrite for the public alert share path that targets `renderPublicAlertMeta`.
  </action>
  <acceptance_criteria>
    - `public/robots.txt` contains `Disallow: /app/`
    - `public/robots.txt` contains `Allow: /public/`
    - `public/sitemap.xml` contains `/public/map`
    - `public/sitemap.xml` contains `/public/alerts`
    - `firebase.json` contains `renderPublicAlertMeta`
  </acceptance_criteria>
  <verify>
    <automated>grep -q "Disallow: /app/" public/robots.txt && grep -q "Allow: /public/" public/robots.txt && grep -q "/public/map" public/sitemap.xml && grep -q "renderPublicAlertMeta" firebase.json</automated>
  </verify>
  <done>Static crawler assets and Hosting rewrite plumbing exist for the release SEO surface.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the published-alert metadata HTTP function and export it</name>
  <files>functions/src/public/renderPublicAlertMeta.ts, functions/src/index.ts</files>
  <read_first>
    - functions/src/index.ts
    - functions/src/announcements/getAnnouncements.ts
    - src/app/public/alerts/detail/page.tsx
    - .planning/phases/12-hardening-pwa-seo-release/12-CONTEXT.md
  </read_first>
  <action>
    Create `functions/src/public/renderPublicAlertMeta.ts` exporting an HTTP function named `renderPublicAlertMeta` that:
    - reads `alertId` from the request path
    - loads `announcements/{alertId}`
    - returns `404` if the document is missing or `status !== 'published'`
    - returns HTML containing alert-specific `<title>`, `<meta name="description">`, `<meta property="og:title">`, `<meta property="og:description">`, `<meta property="og:url">`, and `<link rel="canonical">`
    - includes a browser-safe redirect/bootstrap strategy so non-crawler requests land on the SPA detail page instead of staying on a dead metadata shell

    Update `functions/src/index.ts` to export `renderPublicAlertMeta`.
  </action>
  <acceptance_criteria>
    - `functions/src/public/renderPublicAlertMeta.ts` contains `export const renderPublicAlertMeta`
    - `functions/src/public/renderPublicAlertMeta.ts` contains `status !== 'published'`
    - `functions/src/public/renderPublicAlertMeta.ts` contains `og:title`
    - `functions/src/public/renderPublicAlertMeta.ts` contains `/public/alerts/`
    - `functions/src/index.ts` contains `renderPublicAlertMeta`
  </acceptance_criteria>
  <verify>
    <automated>grep -q "export const renderPublicAlertMeta" functions/src/public/renderPublicAlertMeta.ts && grep -q "status !== 'published'" functions/src/public/renderPublicAlertMeta.ts && grep -q "og:title" functions/src/public/renderPublicAlertMeta.ts && grep -q "renderPublicAlertMeta" functions/src/index.ts</automated>
  </verify>
  <done>Published public alerts have a dedicated server-rendered metadata path for social sharing.</done>
</task>

</tasks>
