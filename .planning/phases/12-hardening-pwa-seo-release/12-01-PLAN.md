---
phase: 12-hardening-pwa-seo-release
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.tsx
  - src/app/router.tsx
  - src/lib/seo/RouteMeta.tsx
  - src/lib/seo/PrivateRouteMeta.tsx
  - src/app/public/landing/page.tsx
  - src/app/public/map/page.tsx
  - src/app/public/alerts/page.tsx
  - src/app/public/alerts/detail/page.tsx
autonomous: true
requirements:
  - SEO-01
  - SEO-05
must_haves:
  truths:
    - "The root route serves a public landing page instead of redirecting into /app"
    - "Public and private routes use explicit metadata helpers rather than one app-wide title"
    - "Every /app/* and /auth/* route can emit noindex metadata without duplicating Helmet markup in each page"
  artifacts:
    - path: "src/lib/seo/RouteMeta.tsx"
      provides: "Reusable public metadata helper"
      contains: "canonicalPath"
    - path: "src/lib/seo/PrivateRouteMeta.tsx"
      provides: "Shared private-route noindex helper"
      contains: "noindex, nofollow"
    - path: "src/App.tsx"
      provides: "Public/private route split"
      contains: "/public/map"
  key_links:
    - from: "src/App.tsx"
      to: "src/lib/seo/PrivateRouteMeta.tsx"
      via: "Private route groups use the shared noindex component"
      pattern: "PrivateRouteMeta"
    - from: "src/App.tsx"
      to: "src/app/public/landing/page.tsx"
      via: "Root route points to the public landing page"
      pattern: "path=\"/\""
---

<objective>
Establish the public route split and reusable SEO/noindex primitives before any public content or OG rewrite work is added.
</objective>

<context>
@.planning/phases/12-hardening-pwa-seo-release/12-CONTEXT.md
@.planning/phases/12-hardening-pwa-seo-release/12-RESEARCH.md
@src/App.tsx
@src/app/router.tsx
@src/app/providers.tsx
@src/app/auth/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shared route metadata helpers for public pages and private noindex routes</name>
  <files>src/lib/seo/RouteMeta.tsx, src/lib/seo/PrivateRouteMeta.tsx</files>
  <read_first>
    - src/App.tsx
    - src/app/providers.tsx
    - .planning/phases/12-hardening-pwa-seo-release/12-CONTEXT.md
    - .planning/phases/12-hardening-pwa-seo-release/12-RESEARCH.md
  </read_first>
  <action>
    Create `src/lib/seo/RouteMeta.tsx` exporting a reusable `RouteMeta` component that renders `title`, `description`, `canonical`, `og:title`, `og:description`, `og:url`, `og:image`, and `twitter:card` tags with `react-helmet-async`. The component must accept explicit props for `title`, `description`, `canonicalPath`, `imagePath`, and `robots`.

    Create `src/lib/seo/PrivateRouteMeta.tsx` exporting a component that renders exactly:
    - `<meta name="robots" content="noindex, nofollow" />`
    - a private-route title suffix that keeps `Bantayog Alert` in the title
  </action>
  <acceptance_criteria>
    - `src/lib/seo/RouteMeta.tsx` contains `canonicalPath`
    - `src/lib/seo/RouteMeta.tsx` contains `og:title`
    - `src/lib/seo/RouteMeta.tsx` contains `twitter:card`
    - `src/lib/seo/PrivateRouteMeta.tsx` contains `noindex, nofollow`
  </acceptance_criteria>
  <verify>
    <automated>grep -q "canonicalPath" src/lib/seo/RouteMeta.tsx && grep -q "og:title" src/lib/seo/RouteMeta.tsx && grep -q "noindex, nofollow" src/lib/seo/PrivateRouteMeta.tsx</automated>
  </verify>
  <done>Reusable SEO helpers exist for public pages and private route noindex behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the root route tree around public pages and protected app routes</name>
  <files>src/App.tsx, src/app/router.tsx, src/app/public/landing/page.tsx, src/app/public/map/page.tsx, src/app/public/alerts/page.tsx, src/app/public/alerts/detail/page.tsx</files>
  <read_first>
    - src/App.tsx
    - src/app/router.tsx
    - src/app/auth/layout.tsx
    - .planning/phases/12-hardening-pwa-seo-release/12-CONTEXT.md
  </read_first>
  <action>
    Update `src/App.tsx` so:
    - `path="/"` renders `LandingPage`
    - `path="/public/map"` renders `PublicMapPage`
    - `path="/public/alerts"` renders `PublicAlertsPage`
    - `path="/public/alerts/:alertId"` renders `PublicAlertDetailPage`
    - the `/app` route tree stays wrapped in `ProtectedRoute`, and its route group renders `PrivateRouteMeta`
    - `/auth/login`, `/auth/register`, and `/auth/profile` also render `PrivateRouteMeta`
    - the final catch-all route redirects to `/`, not `/app`

    Create placeholder page files for the public routes that each render `RouteMeta` with route-specific titles/canonicals and a minimal section container. These placeholders are intentionally skeletal; Phase 12-02 will replace the bodies with the full public content.
  </action>
  <acceptance_criteria>
    - `src/App.tsx` contains `path="/public/map"`
    - `src/App.tsx` contains `path="/public/alerts/:alertId"`
    - `src/App.tsx` contains `PrivateRouteMeta`
    - `src/App.tsx` contains `Navigate to="/" replace`
    - `src/app/public/landing/page.tsx` contains `RouteMeta`
    - `src/app/public/map/page.tsx` contains `canonicalPath="/public/map"`
    - `src/app/public/alerts/page.tsx` contains `canonicalPath="/public/alerts"`
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'path="/public/map"' src/App.tsx && grep -q 'path="/public/alerts/:alertId"' src/App.tsx && grep -q 'PrivateRouteMeta' src/App.tsx && grep -q 'Navigate to="/" replace' src/App.tsx</automated>
  </verify>
  <done>The route tree is split into public and private surfaces with shared metadata scaffolding in place.</done>
</task>

</tasks>
