---
phase: 03-auth-role-model
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/firebase/config.ts
  - src/lib/auth/AuthProvider.tsx
  - src/lib/auth/auth.ts
  - src/lib/auth/providers.ts
  - src/types/user.ts
  - .env.example
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-06
must_haves:
  truths:
    - "User can register with email/password and immediately logs in after email verification"
    - "User can sign in with Google OAuth"
    - "User session persists across browser refresh"
    - "Custom claims (role, municipalityCode, provinceCode) are present in authenticated user ID tokens"
  artifacts:
    - path: src/lib/auth/AuthProvider.tsx
      provides: React context exposing auth state and user custom claims
      min_lines: 50
    - path: src/lib/auth/auth.ts
      provides: Auth initialization and persistence configuration
      exports: browserLocalPersistence, onAuthStateChanged
    - path: src/lib/auth/providers.ts
      provides: Email/password and Google OAuth provider configuration
      exports: emailPasswordProvider, googleProvider
    - path: src/types/user.ts
      provides: UserRole enum and AppUser interface with custom claims types
      contains: UserRole enum, AppUser interface
  key_links:
    - from: src/lib/auth/AuthProvider.tsx
      to: src/lib/firebase/config.ts
      via: imports firebase auth instance
      pattern: import.*firebase/auth
    - from: src/lib/auth/auth.ts
      to: Firebase Auth
      via: browserLocalPersistence
      pattern: setPersistence.*browserLocalPersistence
    - from: src/lib/auth/providers.ts
      to: Firebase Auth
      via: GoogleAuthProvider
      pattern: GoogleAuthProvider
---

<objective>
Initialize Firebase Auth with email/password and Google OAuth providers, configure browserLocalPersistence for session persistence, and create AuthProvider context exposing authenticated user with custom claims.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/lib/firebase/config.ts
@src/types/user.ts
@.env.example

**Existing patterns from Phase 1-2:**
- Firebase config exported as `{ app, auth, db, storage }` from `src/lib/firebase/config.ts`
- TypeScript interfaces colocated with Zod schemas in `src/types/`
- UserRole enum: `citizen`, `municipal_admin`, `provincial_superadmin`
- AppUser interface includes: `id`, `email`, `displayName`, `role`, `municipalityCode`, `provinceCode`, `notificationPreferences`, `createdAt`, `updatedAt`

**Firebase Auth patterns:**
- Use `browserLocalPersistence` (not `sessionLocalPersistence` or `indexedLocalPersistence`) per D-45
- Auth state observed via `onAuthStateChanged`
- Google OAuth uses `signInWithPopup` (not redirect) per D-44
- Default role on registration: `citizen`, `provinceCode='CMN'`, `municipalityCode=null` per D-48
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Auth module with persistence and providers</name>
  <files>src/lib/auth/auth.ts, src/lib/auth/providers.ts</files>
  <read_first>
    src/lib/firebase/config.ts
    src/types/user.ts
  </read_first>
  <action>
    Create `src/lib/auth/auth.ts` with:
    - Import `browserLocalPersistence` from `firebase/auth`
    - Export `configureAuthPersistence()` that calls `setPersistence(auth, browserLocalPersistence)`
    - Export `observeAuthState(auth: Auth, callback: (user: User | null) => void)` using `onAuthStateChanged`
    - Import `auth` from `../firebase/config`

    Create `src/lib/auth/providers.ts` with:
    - Export `googleProvider = new GoogleAuthProvider()` with `signInWithPopup`
    - Export `emailPasswordProvider` config object with `signInWithPopup` method
    - Google provider scopes: `email`, `profile`
    - Custom parameter: `prompt = 'select_account'` to prevent silent re-auth

    **Auth initialization flow:**
    1. `configureAuthPersistence()` called once at app startup before any auth operation
    2. `observeAuthState()` used in AuthProvider to sync React state with Firebase Auth
  </action>
  <verify>
    <automated>grep -n "browserLocalPersistence" src/lib/auth/auth.ts && grep -n "GoogleAuthProvider" src/lib/auth/providers.ts</automated>
  </verify>
  <done>Auth module exports configured persistence and provider instances</done>
  <acceptance_criteria>
    - auth.ts contains `setPersistence(auth, browserLocalPersistence)`
    - auth.ts exports `observeAuthState` function
    - providers.ts exports `googleProvider` instance
    - No TypeScript errors in auth.ts or providers.ts
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create AuthProvider context with custom claims</name>
  <files>src/lib/auth/AuthProvider.tsx, src/lib/auth/index.ts</files>
  <read_first>
    src/lib/firebase/config.ts
    src/types/user.ts
    src/lib/auth/auth.ts
  </read_first>
  <action>
    Create `src/lib/auth/AuthProvider.tsx` that:
    - Imports React, createContext, useContext, useEffect, useState
    - Imports `auth` from `../firebase/config`
    - Imports `observeAuthState` from `./auth`
    - Imports `UserRole` from `../../types/user`
    - Defines `AuthContextValue` interface with:
      - `user: User | null` (Firebase User)
      - `customClaims: { role: UserRole; municipalityCode: string | null; provinceCode: string } | null`
      - `isLoading: boolean`
      - `isAuthenticated: boolean`
    - Creates `AuthContext` with `createContext<AuthContextValue>`
    - Exports `AuthProvider` component that:
      - Calls `configureAuthPersistence()` on mount
      - Uses `observeAuthState` to set user state
      - Extracts custom claims from `user.getIdTokenResult()` then `token.claims`
      - Sets `isLoading` to true until first auth state resolved
    - Provides `{ user, customClaims, isLoading, isAuthenticated }` to context consumers

    Create `src/lib/auth/index.ts` that exports all auth modules:
    ```typescript
    export { AuthProvider } from './AuthProvider'
    export { configureAuthPersistence, observeAuthState } from './auth'
    export { googleProvider, emailPasswordProvider } from './providers'
    ```

    **Custom claims extraction:**
    ```typescript
    const idTokenResult = await user.getIdTokenResult()
    const claims = idTokenResult.claims
    const customClaims = {
      role: claims.role as UserRole,
      municipalityCode: claims.municipalityCode as string | null,
      provinceCode: claims.provinceCode as string,
    }
    ```
  </action>
  <verify>
    <automated>grep -n "AuthContext" src/lib/auth/AuthProvider.tsx && grep -n "customClaims" src/lib/auth/AuthProvider.tsx</automated>
  </verify>
  <done>AuthProvider wraps app and exposes auth state with custom claims to all components</done>
  <acceptance_criteria>
    - AuthProvider.tsx contains AuthContext with customClaims extraction
    - AuthProvider.tsx contains `observeAuthState` usage
    - AuthProvider.tsx wraps children with AuthContext.Provider
    - index.ts exports AuthProvider, configureAuthPersistence, observeAuthState, googleProvider
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Register AuthProvider in App and update .env.example</name>
  <files>src/App.tsx, .env.example</files>
  <read_first>
    src/App.tsx
  </read_first>
  <action>
    Update `src/App.tsx` to:
    - Import `AuthProvider` from `./lib/auth`
    - Wrap the existing Router provider chain with `AuthProvider`
    - Keep existing QueryClientProvider and Router structure intact

    Update `.env.example` to add App Check and Firebase Auth related env vars (for completeness, even though not all are needed immediately):
    ```bash
    # App Check (for Phase 3 - audit mode)
    VITE_FIREBASE_APP_CHECK_KEY=your_app_check_key_here
    ```

    Note: No new env vars needed for basic Firebase Auth since all config is already in firebaseConfig object.
  </action>
  <verify>
    <automated>grep -n "AuthProvider" src/App.tsx</automated>
  </verify>
  <done>AuthProvider wraps entire app, auth state available throughout</done>
  <acceptance_criteria>
    - src/App.tsx imports and wraps with AuthProvider
    - Existing Router and QueryClientProvider structure preserved
    - .env.example contains App Check env var placeholder
  </acceptance_criteria>
</task>

</tasks>

<verification>
- npm run build completes without TypeScript errors
- AuthProvider renders without errors in dev mode
- Auth state changes trigger re-renders in consuming components
</verification>

<success_criteria>
- Firebase Auth initialized with browserLocalPersistence
- Email/password and Google OAuth providers configured
- AuthProvider exposes user + customClaims to all components
- Auth state persists across page refreshes
- Custom claims (role, municipalityCode, provinceCode) extracted from ID token
</criteria>

<output>
After completion, create `.planning/phases/03-auth-role-model/03-01-SUMMARY.md`
</output>
