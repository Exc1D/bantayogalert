---
phase: 01-project-foundation-tooling
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - vite.config.ts
  - index.html
  - src/main.tsx
  - src/App.tsx
autonomous: true
requirements:
  - PWA-02
must_haves:
  truths:
    - "npm run dev starts the Vite dev server without errors"
    - "npm run build produces a production bundle with no TypeScript errors"
    - "App renders a blank white page with title 'Bantayog Alert'"
  artifacts:
    - path: "package.json"
      provides: "All dependency versions pinned, scripts defined"
      contains: "react@18.3.28"
    - path: "tsconfig.json"
      provides: "Strict TypeScript configuration"
      contains: "strict: true, noUncheckedIndexedAccess: true"
    - path: "vite.config.ts"
      provides: "Vite build with @ alias and React plugin"
      contains: "@/*: src/*"
    - path: "src/App.tsx"
      provides: "Minimal placeholder app shell"
      contains: "Bantayog Alert"
    - path: "index.html"
      provides: "HTML entry with correct title"
      contains: "Bantayog Alert"
  key_links:
    - from: "vite.config.ts"
      to: "tsconfig.json"
      via: "alias paths configuration"
      pattern: "@/.*"
---

<objective>
Create the production-ready Vite + React 18 + TypeScript project scaffold with correct dependency versions and strict TypeScript configuration. This is the foundation all subsequent phases build upon.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-project-foundation-tooling/01-CONTEXT.md (D-01 through D-20 for reference)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize package.json with all dependencies</name>
  <files>package.json</files>
  <read_first>/dev/null (greenfield project - no existing file)</read_first>
  <action>
Create package.json with the following pinned versions from CLAUDE.md:

**Dependencies:**
- react@18.3.28
- react-dom@18.3.28
- react-router-dom@6.5.0

**Dev Dependencies:**
- vite@8.0.3
- typescript@6.0.2
- @vitejs/plugin-react@4.3.4
- tailwindcss@3.4.17
- postcss@8.5.3
- autoprefixer@10.4.20
- firebase@12.11.0
- @tanstack/react-query@5.96.2
- zustand@5.0.12
- vite-plugin-pwa@1.2.0
- workbox-window@7.4.0
- zod@4.3.6
- react-helmet-async@3.0.0
- browser-image-compression@2.0.2
- leaflet@1.9.4
- react-leaflet@4.2.1
- @react-leaflet/core@2.1.1
- supercluster@8.0.1
- @types/react@18.3.18
- @types/react-dom@18.3.5
- @types/node@22.10.10
- @types/leaflet@1.9.18
- typescript@6.0.2

**Scripts:**
- dev: "vite"
- build: "tsc -b && vite build"
- preview: "vite preview"
- test: "vitest"
- test:ui: "vitest --ui"
- lint: "eslint . --ext ts,tsx"
- format: "prettier --write ."
- emulators: "firebase emulators:start"
- emulators:ci: "firebase emulators:exec --only auth,firestore,storage,functions -- npm run test"

**Additional devDependencies for tooling:**
- eslint@9.18.0
- @eslint/js@9.18.0
- globals@15.14.0
- typescript-eslint@8.21.0
- prettier@3.4.2
- eslint-plugin-react-hooks@5.1.0
- eslint-plugin-react-refresh@0.4.18
- vitest@4.1.2
- @testing-library/react@16.3.2
- @testing-library/dom@10.4.0
- @playwright/test@1.59.1

**engines:** { "node": ">=20.0.0" }
</action>
  <acceptance_criteria>
    - package.json exists at project root
    - "react": "18.3.28" is present
    - "typescript": "6.0.2" is present
    - "vite": "8.0.3" is present
    - "tailwindcss": "3.4.17" is present
    - "firebase": "12.11.0" is present
    - "npm run build" is defined
    - "npm run dev" is defined
    - "npm run test" is defined
    - "npm run emulators" is defined
  </acceptance_criteria>
  <verify>
    <automated>cat package.json | grep '"react": "18.3.28"' && cat package.json | grep '"typescript": "6.0.2"'</automated>
  </verify>
  <done>package.json created with all pinned dependencies and scripts</done>
</task>

<task type="auto">
  <name>Task 2: Configure tsconfig.json with strict mode</name>
  <files>tsconfig.json</files>
  <read_first>/dev/null (greenfield project)</read_first>
  <action>
Create tsconfig.json with the following configuration:

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "useDefineForClassFields": true,
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "isolatedModules": true,
    "moduleDetection": "force",
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "noUncheckedIndexedAccess": true,
    "resolveJsonModule": true,
    "allowJs": true,
    "forceConsistentCasingInFileNames": true,
    "paths": {
      "@/*": ["./src/*"]
    },
    "types": ["node", "vitest/globals"]
  },
  "include": ["src", "tests"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
```

Also create tsconfig.node.json:
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2023"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "isolatedModules": true,
    "moduleDetection": "force",
    "noEmit": true,
    "strict": true,
    "noUnusedParameters": true
  },
  "include": ["vite.config.ts"]
}
```
</action>
  <acceptance_criteria>
    - tsconfig.json exists with strict: true
    - noUncheckedIndexedAccess: true is present
    - paths.@/* maps to ./src/*
    - noUnusedLocals: true is present
    - noUnusedParameters: true is present
  </acceptance_criteria>
  <verify>
    <automated>cat tsconfig.json | grep -E '"strict": true' && cat tsconfig.json | grep -E '"noUncheckedIndexedAccess": true' && cat tsconfig.json | grep -E '"@/\*"'</automated>
  </verify>
  <done>tsconfig.json created with strict mode, noUncheckedIndexedAccess, and @ alias</done>
</task>

<task type="auto">
  <name>Task 3: Create vite.config.ts with @ alias and React plugin</name>
  <files>vite.config.ts</files>
  <read_first>/dev/null (greenfield project)</read_first>
  <action>
Create vite.config.ts:

```typescript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import path from 'path'

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
  build: {
    target: 'es2022',
    sourcemap: true,
  },
  server: {
    port: 5173,
    host: true,
  },
  test: {
    globals: true,
    environment: 'jsdom',
    setupFiles: ['./src/test/setup.ts'],
    include: ['src/**/*.{test,spec}.{ts,tsx}'],
  },
})
```

The test configuration in the server section is for Vitest integration via @vitejs/plugin-react (not a separate plugin).
</action>
  <acceptance_criteria>
    - vite.config.ts exists at project root
    - resolve.alias.@ maps to ./src
    - plugins contains react()
    - server.port is 5173
    - build.target is es2022
  </acceptance_criteria>
  <verify>
    <automated>cat vite.config.ts | grep -E "@/.*src" && cat vite.config.ts | grep "react()"</automated>
  </verify>
  <done>vite.config.ts created with @ alias and React plugin</done>
</task>

<task type="auto">
  <name>Task 4: Create index.html with correct title</name>
  <files>index.html</files>
  <read_first>/dev/null (greenfield project)</read_first>
  <action>
Create index.html at project root:

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="theme-color" content="#dc2626" />
    <title>Bantayog Alert</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```
</action>
  <acceptance_criteria>
    - index.html exists at project root
    - lang="en" is set
    - title is "Bantayog Alert"
    - meta name="theme-color" content="#dc2626"
    - div#root exists
    - script type="module" src="/src/main.tsx"
  </acceptance_criteria>
  <verify>
    <automated>cat index.html | grep "Bantayog Alert" && cat index.html | grep 'div id="root"'</automated>
  </verify>
  <done>index.html created with correct title and theme color</done>
</task>

<task type="auto">
  <name>Task 5: Create src/main.tsx entry point</name>
  <files>src/main.tsx</files>
  <read_first>/dev/null (greenfield project)</read_first>
  <action>
Create src/main.tsx:

```typescript
import React from 'react'
import ReactDOM from 'react-dom/client'
import { App } from './App'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
)
```

Note: StrictMode is intentionally included — React-Leaflet StrictMode guard is implemented in Phase 4 (DSK/FM shell) to prevent Leaflet double-initialization issues.
</action>
  <acceptance_criteria>
    - src/main.tsx exists
    - React.StrictMode wraps App
    - ReactDOM.createRoot is used
    - getElementById('root')
  </acceptance_criteria>
  <verify>
    <automated>cat src/main.tsx | grep "ReactDOM.createRoot" && cat src/main.tsx | grep "StrictMode"</automated>
  </verify>
  <done>src/main.tsx created as React entry point</done>
</task>

<task type="auto">
  <name>Task 6: Create src/App.tsx placeholder</name>
  <files>src/App.tsx</files>
  <read_first>/dev/null (greenfield project)</read_first>
  <action>
Create src/App.tsx as a minimal placeholder:

```typescript
import { Helmet } from 'react-helmet-async'

export function App() {
  return (
    <>
      <Helmet>
        <title>Bantayog Alert</title>
      </Helmet>
      <div style={{ width: '100vw', height: '100vh', backgroundColor: '#ffffff' }} />
    </>
  )
}
```

This renders a blank white page with the correct title. The actual app shell with routing and providers is created in Plan 04 (Feature Structure & Providers).
</action>
  <acceptance_criteria>
    - src/App.tsx exists
    - Renders a blank white page (100vw x 100vh, white background)
    - Uses react-helmet-async for title
    - Title is "Bantayog Alert"
    - No console errors on render
  </acceptance_criteria>
  <verify>
    <automated>cat src/App.tsx | grep "Bantayog Alert" && cat src/App.tsx | grep "100vh"</automated>
  </verify>
  <done>src/App.tsx created as minimal placeholder</done>
</task>

<task type="auto">
  <name>Task 7: Create src/vite-env.d.ts</name>
  <files>src/vite-env.d.ts</files>
  <read_first>/dev/null (greenfield project)</read_first>
  <action>
Create src/vite-env.d.ts:

```typescript
/// <reference types="vite/client" />
/// <reference types="vite-plugin-pwa/client" />
```
</action>
  <acceptance_criteria>
    - src/vite-env.d.ts exists
    - Contains vite/client reference
    - Contains vite-plugin-pwa/client reference
  </acceptance_criteria>
  <verify>
    <automated>cat src/vite-env.d.ts | grep "vite/client"</automated>
  </verify>
  <done>src/vite-env.d.ts created with Vite type references</done>
</task>

<task type="auto">
  <name>Task 8: Create src/test/setup.ts for Vitest</name>
  <files>src/test/setup.ts</files>
  <read_first>/dev/null (greenfield project)</read_first>
  <action>
Create src/test/setup.ts for Vitest:

```typescript
import '@testing-library/jest-dom'
import { afterEach } from 'vitest'
import { cleanup } from '@testing-library/react'

afterEach(() => {
  cleanup()
})
```
</action>
  <acceptance_criteria>
    - src/test/setup.ts exists
    - Imports @testing-library/jest-dom
    - Exports afterEach cleanup
  </acceptance_criteria>
  <verify>
    <automated>cat src/test/setup.ts | grep "jest-dom"</automated>
  </verify>
  <done>src/test/setup.ts created for Vitest</done>
</task>

</tasks>

<verification>
- npm ci succeeds and installs all dependencies
- npm run dev starts Vite dev server on port 5173
- npm run build produces production bundle in dist/
- No TypeScript errors from tsc
</verification>

<success_criteria>
- Dev server starts without errors
- Production build completes with no errors
- TypeScript compilation passes with strict mode
- App renders blank white page with correct title
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-foundation-tooling/01-01-SUMMARY.md`
</output>
