---
phase: 01-project-foundation-tooling
plan: '02'
type: execute
wave: 1
depends_on: []
files_modified:
  - eslint.config.js
  - .prettierrc
  - .prettierignore
  - tailwind.config.js
  - postcss.config.js
  - .env.example
  - public/
autonomous: true
requirements: []
must_haves:
  truths:
    - "ESLint runs and passes with no errors on the scaffold"
    - "Prettier formats all source files consistently"
    - "Tailwind CSS is configured with disaster/emergency color semantics"
    - ".env.example contains all required Firebase configuration keys"
  artifacts:
    - path: "eslint.config.js"
      provides: "ESLint v9 flat config with React hooks plugin"
      contains: "react-hooks"
    - path: ".prettierrc"
      provides: "Prettier configuration"
      contains: "printWidth: 100"
    - path: "tailwind.config.js"
      provides: "Tailwind v3 config with disaster color semantics"
      contains: "severity-critical, severity-warning"
    - path: ".env.example"
      provides: "Firebase environment variable template"
      contains: "VITE_FIREBASE_"
    - path: "postcss.config.js"
      provides: "PostCSS configuration for Tailwind"
      contains: "tailwindcss"
---

<objective>
Configure ESLint, Prettier, Tailwind CSS v3, and environment variable templates. This provides the code quality tooling and styling foundation that all subsequent phases use.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-project-foundation-tooling/01-CONTEXT.md (D-18, D-19, D-20, D-65)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ESLint v9 flat config</name>
  <files>eslint.config.js</files>
  <read_first>/dev/null (greenfield project)</read_first>
  <action>
Create eslint.config.js using ESLint v9 flat config format:

```javascript
import js from '@eslint/js'
import globals from 'globals'
import tseslint from 'typescript-eslint'
import reactHooks from 'eslint-plugin-react-hooks'
import reactRefresh from 'eslint-plugin-react-refresh'
import react from 'eslint-plugin-react'

export default tseslint.config(
  { ignores: ['dist', 'node_modules', '*.config.js'] },
  {
    extends: [js.configs.recommended],
    files: ['**/*.{ts,tsx}'],
    languageOptions: {
      ecmaVersion: 2022,
      globals: {
        ...globals.browser,
        ...globals.node,
      },
      parserOptions: {
        ecmaVersion: 'latest',
        sourceType: 'module',
        ecmaFeatures: { jsx: true },
      },
    },
    plugins: {
      react,
      'react-hooks': reactHooks,
      'react-refresh': reactRefresh,
    },
    rules: {
      ...reactHooks.configs.recommended.rules,
      'react-refresh/only-export-components': [
        'warn',
        { allowConstantExport: true },
      ],
      'react/react-in-jsx-scope': 'off',
      'react/prop-types': 'off',
      '@typescript-eslint/no-unused-vars': ['error', { argsIgnorePattern: '^_' }],
    },
  },
)
```
</action>
  <acceptance_criteria>
    - eslint.config.js exists at project root
    - Uses ESLint v9 flat config (typescript-eslint v8)
    - Includes react-hooks plugin
    - Includes react-refresh plugin
    - Ignores dist and node_modules
    - NoUnusedVars rule applied
  </acceptance_criteria>
  <verify>
    <automated>cat eslint.config.js | grep "typescript-eslint" && cat eslint.config.js | grep "react-hooks"</automated>
  </verify>
  <done>ESLint v9 flat config created</done>
</task>

<task type="auto">
  <name>Task 2: Create Prettier config</name>
  <files>.prettierrc, .prettierignore</files>
  <read_first>/dev/null (greenfield project)</read_first>
  <action>
Create .prettierrc:

```json
{
  "printWidth": 100,
  "tabWidth": 2,
  "useTabs": false,
  "semi": true,
  "singleQuote": true,
  "quoteProps": "as-needed",
  "jsxSingleQuote": false,
  "trailingComma": "es5",
  "bracketSpacing": true,
  "bracketSameLine": false,
  "arrowParens": "always",
  "endOfLine": "lf",
  "plugins": []
}
```

Create .prettierignore:

```
node_modules
dist
.firebase
*.lock
package-lock.json
yarn.lock
pnpm-lock.yaml
```

Note: No Prettier plugins needed — standard Prettier ecosystem.
</action>
  <acceptance_criteria>
    - .prettierrc exists at project root
    - printWidth is 100
    - singleQuote is true
    - .prettierignore exists
    - Ignores node_modules, dist, package-lock.json
  </acceptance_criteria>
  <verify>
    <automated>cat .prettierrc | grep '"printWidth": 100' && cat .prettierrc | grep '"singleQuote": true'</automated>
  </verify>
  <done>Prettier configuration created</done>
</task>

<task type="auto">
  <name>Task 3: Create Tailwind CSS v3 configuration</name>
  <files>tailwind.config.js, postcss.config.js</files>
  <read_first>/dev/null (greenfield project)</read_first>
  <action>
Create tailwind.config.js (Tailwind v3, NOT v4 — D-18):

```javascript
/** @type {import('tailwindcss').Config} */
export default {
  content: ['./src/**/*.{ts,tsx}', './index.html'],
  darkMode: 'class',
  theme: {
    extend: {
      colors: {
        'severity-critical': '#dc2626',
        'severity-warning': '#f59e0b',
        'severity-info': '#3b82f6',
        'severity-clear': '#22c55e',
      },
      fontFamily: {
        sans: ['Inter', 'system-ui', 'sans-serif'],
      },
    },
  },
  plugins: [],
}
```

Create postcss.config.js:

```javascript
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
```
</action>
  <acceptance_criteria>
    - tailwind.config.js exists at project root
    - content paths include ./src/**/*.{ts,tsx}
    - darkMode is 'class' (D-20 — class strategy, NOT media)
    - severity colors defined: severity-critical (red-600), severity-warning (amber-500), severity-info (blue-500), severity-clear (green-500)
    - postcss.config.js exists
    - postcss.config.js uses tailwindcss and autoprefixer plugins
  </acceptance_criteria>
  <verify>
    <automated>cat tailwind.config.js | grep "darkMode" && cat tailwind.config.js | grep "severity-critical" && cat tailwind.config.js | grep "content"</automated>
  </verify>
  <done>Tailwind CSS v3 and PostCSS configuration created</done>
</task>

<task type="auto">
  <name>Task 4: Create .env.example with Firebase config keys</name>
  <files>.env.example</files>
  <read_first>/dev/null (greenfield project)</read_first>
  <action>
Create .env.example with the following Firebase configuration keys (D-05, D-06):

```
# Firebase Configuration (D-05: Single Firebase project, environment overlays)
# Copy to .env.local for local development

# Web API Key
VITE_FIREBASE_API_KEY=your_api_key_here

# Auth Domain
VITE_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com

# Firestore Database URL
VITE_FIREBASE_DATABASE_URL=https://your_project.firebaseio.com

# Project ID
VITE_FIREBASE_PROJECT_ID=your_project_id

# Storage Bucket
VITE_FIREBASE_STORAGE_BUCKET=your_project.appspot.com

# Messaging Sender ID
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id

# App ID
VITE_FIREBASE_APP_ID=your_app_id

# Measurement ID (optional)
VITE_FIREBASE_MEASUREMENT_ID=your_measurement_id
```

Note: These are client-side Vite env vars (prefixed with VITE_). Server-side vars would not have the prefix but are managed in firebase.json for Cloud Functions.
</action>
  <acceptance_criteria>
    - .env.example exists at project root
    - Contains VITE_FIREBASE_API_KEY
    - Contains VITE_FIREBASE_AUTH_DOMAIN
    - Contains VITE_FIREBASE_PROJECT_ID
    - Contains VITE_FIREBASE_STORAGE_BUCKET
    - Contains VITE_FIREBASE_MESSAGING_SENDER_ID
    - Contains VITE_FIREBASE_APP_ID
    - Contains comment about copying to .env.local
  </acceptance_criteria>
  <verify>
    <automated>cat .env.example | grep "VITE_FIREBASE_API_KEY" && cat .env.example | grep "VITE_FIREBASE_PROJECT_ID"</automated>
  </verify>
  <done>.env.example created with all Firebase configuration keys</done>
</task>

<task type="auto">
  <name>Task 5: Create public directory with placeholder favicon</name>
  <files>public/favicon.svg</files>
  <read_first>/dev/null (greenfield project)</read_first>
  <action>
Create public/favicon.svg — a simple red alert/shield icon:

```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100">
  <circle cx="50" cy="50" r="45" fill="#dc2626"/>
  <path d="M50 20L70 75H30L50 20Z" fill="white" opacity="0.9"/>
  <circle cx="50" cy="55" r="5" fill="#dc2626"/>
  <rect x="47" y="62" width="6" height="15" rx="1" fill="#dc2626"/>
</svg>
```

This is a simple placeholder — Phase 12 (Hardening) will refine icons and manifest. The theme color #dc2626 matches the severity-critical red.
</action>
  <acceptance_criteria>
    - public/favicon.svg exists
    - Uses viewBox="0 0 100 100"
    - Theme color matches #dc2626
  </acceptance_criteria>
  <verify>
    <automated>cat public/favicon.svg | grep "#dc2626"</automated>
  </verify>
  <done>public/favicon.svg created as placeholder</done>
</task>

</tasks>

<verification>
- npm run lint runs ESLint without errors
- npm run format formats all files
- npm run build uses Tailwind CSS correctly (no unknown class errors)
</verification>

<success_criteria>
- ESLint passes on scaffold files
- Prettier produces consistent formatting
- Tailwind CSS generates correct utility classes
- .env.example is complete for Firebase setup
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-foundation-tooling/01-02-SUMMARY.md`
</output>
