---
phase: 08-contacts-management
plan: "08-04"
type: execute
wave: 3
depends_on:
  - "08-03"
requirements:
  - CON-01
  - CON-02
  - CON-03
  - CON-04
  - CON-05
files_modified:
  - src/app/router.tsx
  - src/app/shell/DesktopNavRail.tsx
  - src/app/shell/MobileBottomTabs.tsx
  - src/app/contacts/page.tsx
autonomous: true
must_haves:
  truths:
    - "Contacts page is accessible at /app/contacts"
    - "Contacts nav item appears in desktop nav rail for municipal_admin and provincial_superadmin roles"
    - "Contacts nav item appears in mobile bottom tabs for municipal_admin and provincial_superadmin roles"
    - "Unauthenticated and citizen users cannot access /app/contacts"
  artifacts:
    - path: src/app/contacts/page.tsx
      provides: Contacts page route component
    - path: src/app/shell/DesktopNavRail.tsx (modified)
      provides: Contacts nav item for admin roles
    - path: src/app/shell/MobileBottomTabs.tsx (modified)
      provides: Contacts nav item for admin roles on mobile
---

<objective>
Router and navigation integration for contacts. This plan adds the /app/contacts route and updates the nav components to show the Contacts link for admin users.
</objective>

<context>
@src/app/router.tsx -- Existing router configuration
@src/app/shell/DesktopNavRail.tsx -- Existing nav rail
@src/app/shell/MobileBottomTabs.tsx -- Existing mobile tabs
@src/components/contacts/ContactsPage.tsx -- Already created in 08-03
</context>

<read_first>
- src/app/router.tsx
- src/app/shell/DesktopNavRail.tsx
- src/app/shell/MobileBottomTabs.tsx
- src/components/contacts/ContactsPage.tsx
</read_first>

<tasks>

<task type="auto">
  <name>Task 1: Create src/app/contacts/page.tsx</name>
  <files>src/app/contacts/page.tsx</files>
  <action>
Create `src/app/contacts/page.tsx`:

```typescript
import { ContactsPage } from '@/components/contacts/ContactsPage'
import { isAdmin } from '@/lib/auth/guards'

export default async function ContactsRoute() {
  // Server-side admin check - redirects if not admin
  await isAdmin()

  return <ContactsPage />
}
```

This is a server component that wraps the ContactsPage. The `isAdmin()` guard checks the user's role and redirects to login if not authenticated or to the appropriate page if not an admin.
  </action>
  <verify>grep -r "ContactsPage" src/app/contacts/page.tsx && grep -r "isAdmin" src/app/contacts/page.tsx</verify>
  <done>Contacts route exists at src/app/contacts/page.tsx</done>
</task>

<task type="auto">
  <name>Task 2: Update DesktopNavRail to include Contacts nav item for admins</name>
  <files>src/app/shell/DesktopNavRail.tsx</files>
  <action>
Read the existing DesktopNavRail.tsx to understand the current nav items structure.

Then update the nav items array to include a Contacts item for admin roles:

```typescript
import { Phone, Users, BarChart3, ScrollText } from 'lucide-react'
// Add: import { Contact } from 'lucide-react' if not already imported

// In the nav items definition, add:
{
  id: 'contacts',
  label: 'Contacts',
  icon: ContactIcon,  // You'll need to import an icon, e.g., Users or create a Contact icon
  href: '/app/contacts',
  roles: ['municipal_admin', 'provincial_superadmin'] as const,
}
```

The existing role-based nav item pattern (from Phase 04) already supports filtering by role. Add the Contacts item to the appropriate list of nav items, ensuring it only appears for municipal_admin and provincial_superadmin roles.
  </action>
  <verify>grep -r "contacts" src/app/shell/DesktopNavRail.tsx</verify>
  <done>DesktopNavRail includes Contacts nav item for admin roles</done>
</task>

<task type="auto">
  <name>Task 3: Update MobileBottomTabs to include Contacts nav item for admins</name>
  <files>src/app/shell/MobileBottomTabs.tsx</files>
  <action>
Read the existing MobileBottomTabs.tsx to understand the current tab structure.

Then add a Contacts tab for admin roles, following the same pattern as other admin-only tabs (like Dashboard or Analytics):

```typescript
// Add Contacts tab item for admin roles
{
  id: 'contacts',
  label: 'Contacts',
  icon: ContactIcon,
  href: '/app/contacts',
  roles: ['municipal_admin', 'provincial_superadmin'] as const,
}
```

The existing role-based filtering pattern should already handle showing/hiding based on the user's role from useAuth().
  </action>
  <verify>grep -r "contacts" src/app/shell/MobileBottomTabs.tsx</verify>
  <done>MobileBottomTabs includes Contacts tab for admin roles</done>
</task>

<task type="auto">
  <name>Task 4: Update router.tsx to add /app/contacts route</name>
  <files>src/app/router.tsx</files>
  <action>
Update `src/app/router.tsx` to add the contacts route:

Read the existing router.tsx first, then add:

```typescript
{
  path: '/app/contacts',
  lazy: () => import('./contacts/page').then((m) => ({ Component: m.default })),
}
```

The route should be placed within the app shell routes that require authentication. Use lazy loading for code splitting.

The route guard (AdminRoute or isAdmin check) should be applied either here or in the page component itself.
  </action>
  <verify>grep -r "/app/contacts" src/app/router.tsx</verify>
  <done>Router includes /app/contacts route with lazy loading</done>
</task>

</tasks>

<verification>
- `npm run build` -- TypeScript compiles without errors
- `grep -r "contacts" src/app/shell/DesktopNavRail.tsx` -- nav updated
- `grep -r "contacts" src/app/shell/MobileBottomTabs.tsx` -- tabs updated
- `grep -r "/app/contacts" src/app/router.tsx` -- route exists
- `grep -r "ContactsPage" src/app/contacts/page.tsx` -- page exists
</verification>

<success_criteria>
- src/app/contacts/page.tsx: Route component wrapping ContactsPage
- src/app/router.tsx: /app/contacts route added with lazy loading
- src/app/shell/DesktopNavRail.tsx: Contacts nav item for admin roles
- src/app/shell/MobileBottomTabs.tsx: Contacts tab for admin roles
</success_criteria>

<output>
After completion, create `.planning/phases/08-contacts-management/08-04-SUMMARY.md`
</output>
