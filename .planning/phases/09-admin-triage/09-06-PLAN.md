---
phase: 09-admin-triage
plan: "06"
type: execute
wave: 4
depends_on: ["09-05"]
files_modified:
  - src/app/router.tsx
  - src/app/DesktopShell.tsx
  - src/app/MobileShell.tsx
autonomous: true
requirements:
  - TRI-01
  - TRI-15
  - TRI-16

must_haves:
  truths:
    - "Admin queue is accessible via /app/admin route"
    - "Desktop nav rail shows Admin Queue item for municipal_admin and provincial_superadmin"
    - "Mobile shows Admin section in profile for admin roles"
    - "ProvincialSuperadmin default municipality filter is null (all municipalities)"
  artifacts:
    - path: "src/app/router.tsx"
      provides: "Route /app/admin rendering AdminQueueFeed"
    - path: "src/app/DesktopShell.tsx"
      provides: "Admin Queue nav item for admin roles"
    - path: "src/app/MobileShell.tsx"
      provides: "Admin access in mobile profile section"
---

<objective>
Wire up the admin queue to the routing and navigation system. Add the /app/admin route, update DesktopNavRail and MobileShell to show admin queue navigation for municipal_admin and provincial_superadmin roles. This completes the phase integration.
</objective>

<context>
@src/app/router.tsx
@src/app/DesktopShell.tsx
@src/app/MobileShell.tsx
@src/lib/auth/AuthProvider.tsx
@src/stores/uiStore.ts

## Navigation for Admin Roles

Per D-192: provincial_superadmin sees all 12 municipalities with filter dropdown.
Per D-198: municipal_admin sees only own municipality's reports.

DesktopNavRail items for admin roles:
- Map, Feed (visible to all)
- Dashboard (admin only)
- Admin Queue (admin only) — TRI-01
- Contacts (admin only — from Phase 08)
- Analytics (future — Phase 11)
- Audit Log (future — Phase 11)
- Alerts (all roles)
- Profile (all roles)

Mobile: Admin access via Profile → Admin Panel section (D-116: "Admin functions accessible via Profile → Admin Panel section")
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update router.tsx — add /app/admin route</name>
  <files>src/app/router.tsx</files>
  <action>
    Update `src/app/router.tsx` to add the /app/admin route.

    Add import for AdminQueueFeed:
    ```typescript
    import { AdminQueueFeed } from '@/components/report/AdminQueueFeed'
    ```

    Add route:
    ```typescript
    {
      path: '/app/admin',
      element: <AdminQueueFeed />,
    }
    ```

    Note: No auth guard in router.tsx — auth guards are handled by ProtectedRoute components in the shell (Phase 03 pattern). The AdminQueueFeed itself checks role via useAuth() and shows appropriate error if not admin.
  </action>
  <verify>
    <automated>grep -n "/app/admin\|AdminQueueFeed" /home/exxeed/dev/projects/bantayogalert/src/app/router.tsx</automated>
  </verify>
  <done>Router has /app/admin route rendering AdminQueueFeed</done>
</task>

<task type="auto">
  <name>Task 2: Update DesktopShell.tsx — add Admin Queue nav item for admin roles</name>
  <files>src/app/DesktopShell.tsx</files>
  <action>
    Read DesktopShell.tsx to understand the current nav rail structure.

    Add "Admin Queue" nav item to DesktopNavRail for admin roles.

    The nav item should:
    - Be visible only when customClaims.role === 'municipal_admin' or 'provincial_superadmin'
    - Use an appropriate icon (e.g., clipboardList or shield from lucide-react)
    - Link to /app/admin
    - Show active state when current path starts with /app/admin

    Place it in the nav rail between "Map/Feed" section and "Contacts" (Phase 08) item, or after existing items.

    Look at existing nav item implementation in DesktopShell.tsx for the pattern to follow.
  </action>
  <verify>
    <automated>grep -n "Admin.*Queue\|clipboardList\|shield" /home/exxeed/dev/projects/bantayogalert/src/app/DesktopShell.tsx</automated>
  </verify>
  <done>DesktopShell nav rail shows Admin Queue for municipal_admin and provincial_superadmin roles</done>
</task>

<task type="auto">
  <name>Task 3: Update MobileShell.tsx — add Admin Panel access for admin roles</name>
  <files>src/app/MobileShell.tsx</files>
  <action>
    Read MobileShell.tsx to understand the current mobile layout.

    Per D-116: "Admin functions accessible via Profile → Admin Panel section".

    In the Profile tab (or bottom tab bar if applicable), add an "Admin Panel" section for admin roles.

    If MobileShell has a Profile screen/tab:
    - Add "Admin Panel" button/section visible to municipal_admin and provincial_superadmin
    - Button links to /app/admin

    If there is a separate mobile admin section or bottom tab for admin:
    - Add appropriate nav item

    Look at the current Profile implementation in MobileShell.tsx for the pattern to follow.
  </action>
  <verify>
    <automated>grep -n "Admin\|admin\|Profile" /home/exxeed/dev/projects/bantayogalert/src/app/MobileShell.tsx | head -20</automated>
  </verify>
  <done>MobileShell has Admin Panel access point for admin roles</done>
</task>

</tasks>

<verification>
- /app/admin route renders AdminQueueFeed
- DesktopNavRail shows Admin Queue for admin roles
- MobileShell has Admin Panel section/link for admin roles
- Superadmin sees all municipalities by default (filterMunicipality=null in AdminQueueFeed)
- Municipal admin is automatically scoped to own municipality
</verification>

<success_criteria>
- Admin queue accessible via navigation from desktop and mobile
- Admin roles see Admin Queue nav item, citizens do not
- Superadmin default view is all municipalities
- Municipal admin is automatically scoped to own municipality
- No authentication/authorization bypasses — all enforced via useAuth() and CF validation
</success_criteria>

<output>
After completion, create `.planning/phases/09-admin-triage/09-06-SUMMARY.md`
</output>
